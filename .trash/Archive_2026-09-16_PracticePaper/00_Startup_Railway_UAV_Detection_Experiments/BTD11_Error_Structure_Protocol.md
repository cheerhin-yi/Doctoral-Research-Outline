# BTD11：F1280剩余错误的合并缓存筛查

2026-09-14，BTD11-ERROR-20260914-01。用户要求续接未完成任务；BTD10已完成，不重跑其工作。本次仅CPU读取现有cal48缓存，模型加载／训练／推理次数均须为0。

输入：BTD8的48份reference_F1280预测（推理conf下限.001、经过框架NMS及公共NMS/500上限），BTD9匹配结果，BTD1图像身份及GT元数据，相同cal48原始八字段标注。按原manifest逐份核SHA；不读取diag500/test-dev或模型权重。图片仅用于少量案例目视复核。

## 固定规则

维持原评价：所有尺寸共同匹配、类别一一对应、正常GT优先于可复用score=0 GT、同IoU取后GT、区域ignore语义、原图面积<1024。基准conf=.25/IoU=.5，核对小TP1312、小FN1408、FP839。新函数增加预测匹配轨迹，不修改原评价器；基准需逐图与旧函数及BTD9集合一致。

漏检证据使用通过区域ignore的已保存预测，对正常GT计算普通IoU；高分>=.25，低分[.001,.25)，接近定位区间[.1,.5)，符合定位>=.5。每个GT保存多项独立标志，互斥汇总按如下次序，故组名只表示优先观察证据，不是因果：

1. 高分同类且IoU>=.5但未匹配：匹配竞争；
2. 低分同类且IoU>=.5：低分可见；
3. 高分同类接近定位：定位不足；
4. 高分异类且IoU>=.5：类别不符；
5. 高分异类接近定位：类别／定位混合；
6. 仅低分同类接近定位、低分异类定位符合、低分异类接近定位（依此次序分组）；
7. 以上均无：保存输出中未见IoU>=.1候选。不得解释为网络无响应或NMS误删。

FP按重复（同类IoU>=.5）、定位（同类[.1,.5)）、错类（异类>=.5）、混合（异类[.1,.5)）、背景依次分类；只对原评价器标为FP的预测分解，不改变其总数。无官方TIDE/AP主张。

## 有限对照和诊断修复

固定阈值对照=.25/.10/.05/.01/.001；不选择最佳阈值作为新方法。每项报告小TP、补回／丢失、全尺寸TP/FP/Precision；所有对照使用同一已截断预测。

三个GT诊断分别从原预测独立出发：定位只移动高分同类且IoU∈[.1,.5)的框到目标GT；分类只改高分异类且IoU>=.5的类别；分数只把低分同类IoU>=.5的分数提高至.25。仅使用未占用基准TP的预测，对原小FN做最大基数二分匹配，一框最多对应一GT，不改已有TP框。确定性顺序：GT候选数升序／行号，预测分数降序／索引。最大匹配数是“锁定基准TP且限定证据边时的一对一容量”，不是可部署改善。

保存选定边、修复后数组和实际重评集合。真实重评可能出现竞争／原TP损失，必须披露；确定性修复结果不冒充最优效果上界，各项不相加。修复在最终预测上执行，不重跑NMS，不能代替改训练或改NMS的实验。

同时报告尺寸、类别、同类GT最大IoU邻接组（<.1、[.1,.5)、>=.5）的完整分母／检出／漏检，观察集中度。邻接GT只用于诊断，不能成为推理输入。挑选各主要组与反例的固定代表，保存原图及GT行号定位，不改标签。

## 资源、验证和决定

运行CPU总计10分钟、RSS≤2GiB、系统可用≥256MiB、磁盘剩余≥2GiB、输出≤128MiB；每图前及结果后采样并立即落盘，非硬实时资源上限。任何身份、集合、几何或资源失败停止，保留失败目录，不覆盖旧产物。代码依赖仅既有NumPy/psutil及CPU评价工具，不安装训练依赖。

先用人工样例校核正常GT优先、ignore可复用、同分/同IoU顺序、预测竞争及一对一最大匹配；随后48图逐图核旧匹配。独立验证输出哈希、1408条漏检唯一性、分组守恒、每个修复一对一及只改一个属性、逐图／类别／FP总和。

完成后一次性给出：继续形成单机制方案、证据不足需明确取证，或停止当前探索。可见低分／定位误差多不等于已有独立创新；还需推理可得信号和直接近邻差异。若只有普通阈值的召回—误检交换，或候选缺失／不可观察占据主要空间，不自动再训练、扩大数据、加模块或衍生无止境诊断。
