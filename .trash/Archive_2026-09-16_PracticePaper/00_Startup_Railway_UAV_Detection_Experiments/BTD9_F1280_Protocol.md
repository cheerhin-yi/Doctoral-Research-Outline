# BTD9：F1280强基线缓存效果对照

2026-09-14，`BTD9-F1280-20260914-01`。仅使用BTD8已保存的cal48 F1280最终预测、K1结果/全部单片匹配集合、BTD1全图640结果及相同原标注。不加载模型、不推理/训练、不改阈值或分辨率、不新增diag500/test-dev访问。

先核验产物清单SHA和cal48身份。F1280预测已经过公共NMS及500框上限，直接按既有match_gt评价，不额外NMS。沿用conf=.25、匹配IoU=.5、原图面积<1024、所有尺寸一起匹配及既有ignore语义。复核已有F640/K1/GT最佳单片小TP、TP/FP及原标注行号集合，不改变最佳单片的同分规则或FP目标。

交付四配置的小TP/Recall、全尺寸TP/FP/Precision/Recall，F1280相对其他配置逐图和逐类补回/丢失，保存原GT行号与框。报告F1280与单片的共同/各自独有检出，不把集合并集当作实际融合结果。用BTD8实际F1280/K1时间作配对背景，F640未在该计时批次测，不拼接旧时间；GT最佳单片没有对应实际选择成本或完整计时。

同时报告逐图GT选择F1280或最佳单片的计数上界（各图取小TP较高者，同分F1280），只用于判断互补是否能在单一动作中利用。跨窗口/跨方法集合并集仅为逐目标覆盖诊断，不能解释为同时可达。所有GT选择均不可部署。

结论限制：总小TP较高不等于集合包含关系，TP/FP及类别交换必须同时披露。当前cal48是反复接触的开发集，不声称官方AP、统计确认、独立泛化或论文创新。若F1280已覆盖/明显强于当前选择上界，暂缓现有区域排序新机制；若仍有互补，先定位而不自动训练新网络或扩大边界。

资源：CPU每图前记录RSS/可用内存，10分钟、RSS≤2GiB、系统可用≥256MiB、磁盘余量≥2GiB，产物≤128MiB逐图检查。样本立即落盘，非连续硬上限。任何身份、守恒或几何错误停止并保留失败；不覆盖旧记录。验收48图、四配置计数与配对恒等式、逐类/逐图表、输入SHA/脚本快照、结果和唯一后续任务。
