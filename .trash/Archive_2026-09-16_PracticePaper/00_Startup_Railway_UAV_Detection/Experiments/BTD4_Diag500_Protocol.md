# BTD4：diag500两种简单选区配对验证与完整测速

2026-09-13，Run ID `BTD4-DIAG500-20260913-01`；用户明确要求继续本任务。先记录条件再运行，不重新训练或搜索参数。

- 数据：固定val ZIP SHA `abeea063037e5d20398837deb11084e652402a34ddf4f207bdf541a6f2a35ef9`；按已有`diag-v1:`文件名SHA排序，前48张cal48，余500张diag500，逐名核对既有清单。仅读原val包，不接触test-dev，不改原标注。cal48已用于开发，diag500是预留开发子集，不是来源独立测试。
- 权重：第100轮last，SHA `bc42d54e37acaf1f698af487439dc222fa86fb4498623e8cf954df14e0aa5533`；YOLO11n十类，固定UAV_BT1环境，GTX1660SUPER，batch1、FP32，640方形letterbox。每视图conf=.001/max1000，最终按类NMS=.5/max500。
- 两个方法仅选第二片不同：原密度Top2；去重密度第一片相同，第二片统计第一片未覆盖的粗预测中心，排除第一片，同分按原窗口索引。粗预测conf≥.25；都用原640窗口/512步长/贴边、最多两片，候选不足两片则取全部。此不足两片处理是既有通用协议要求，不改变多窗口排序。零分仍取固定索引补足；不读GT选区。
- 准确率：每种方法每图第一次实际运行的预测固定为统计输入；全部尺寸匹配后统计原图面积<1024小GT，conf=.25/IoU=.5与既有ignore语义一致，报告TP/FP、Recall、各类、逐图补回/丢失。保存低阈值最终预测，但本轮不声称官方AP或正式评测完成。
- 计时：已解码CPU RGB到最终CPU原图坐标框；包括RGB→框架BGR转换、窗口生成、全图检测、选区、裁片/全部局部检测/回映、最终合并。前后GPU同步，图像读取解码另记。准确率匹配、日志写盘和数据归档在计时外；不报告相机到输出延迟。
- 实跑：cal48前10图只检查当前两套完整流程及F1280计时参考通路；不是重做旧训练/效果实验。每配置预热20次；cal48每配置计时3次，diag500两方法各3次。每图每轮交替谁先运行。每次重新前向，禁止缓存代替耗时。计时重复不计为独立样本，统计一套准确率；重复输出一致性另核验。
- 预算参考T：沿用既有最小协议，以cal48 F1280的每图3次中位延迟的p95向上取整到1ms。F1280只作时间参考，不新增准确率竞赛，不以此反向调整K或阈值。它不是巡检业务截止时间。报告两方法均值/中位/p95、逐图中位分布和全部1500帧次分布，以及超过T的比例；若超时如实报告，不宣称硬实时。
- 统计：固定图像级配对自助抽样10000次seed20260913，报告小目标Recall差范围；来源组未知，不能解释为独立场景的确认性区间。报告变化集中度与反例，不挑图。
- 停止：OOM、非有限值、数据身份/几何/类别错误立即保留失败；不自动降低分辨率/改精度。运行上限2小时，产物10GiB，磁盘至少5GiB。无云端启动、模型训练、网络修改、额外弱响应或新模块。

验收：500图/两方法各1500次实际计时、96+1000次两方法首次准确率结果（cal48不合入diag500精度）、固定数据和权重身份、完整分项与总时延、逐GT增减可回链，给出简单对照是否值得保留的结论。缺任何图或关键计时则不得宣称完整完成。
