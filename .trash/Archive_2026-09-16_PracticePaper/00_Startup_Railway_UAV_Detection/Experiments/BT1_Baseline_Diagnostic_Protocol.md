# BT1基线归档与cal48漏检诊断协议

登记日期：2026-09-13；Run ID：`BTD1-CAL48-20260913-01`。用户明确要求归档基线并分析漏检是否支持候选。本次为已批准受限开发路径中的一次小规模诊断，先固定以下条件再运行。

- 权重：第100轮`BT1-LOCAL-20260913-01/train/weights/last.pt`，SHA256 `bc42d54e37acaf1f698af487439dc222fa86fb4498623e8cf954df14e0aa5533`；不使用best。
- 数据：既有cal48全48图固定顺序；不读diag500或test-dev图像、标注。不训练、不改变网络，不搜参数。cal48已用于训练监控，只产生探索性证据。
- 模型：独立UAV_BT1环境、GTX1660SUPER、batch1、FP32、640方形letterbox（rect=false），每视图conf=.001、IoU=.5、max_det=1000；最终类别NMS=.5，全类别最多500框。稳定等分顺序为视图、预测原索引。
- 局部池：原图640窗口、512步长、末端贴边、短边不足补114至640，排除补边中心并裁回有效边界。全图与局部共享权重、阈值及融合。
- 固定控制：F640、所有局部Tall（允许超预算，仅诊断参考），以及K≤2的密度、低阈值峰计数(.25/.05/.01/.001)、匹配完整候选正峰总数的原分数Top-M、P3-only、同类原始跨层分数乘积、半幅循环移位P4、完整候选。
- 候选按已有v0.1：P3最高类别3×3局部极大、同分取较早格点；9×9/5×5去中心有效邻域MAD下限.1，z裁到[0,6]；最近P4格点同类支持；全图≥.25同类框覆盖的峰统一剔除；每区累加最高8峰。低阈值控制计数而非强度求和，密度按最终框中心计数。所有对照仅用预测选区。原分数乘积只保留同类，移位只在有效P4矩形循环移动数值和类别，不移动padding；无正得分区域不选（密度按历史规则零分仍取固定前两片）。
- 诊断匹配：原始八字段GT，复用已核验ignore语义；先按类别匹配全部尺寸，再统计0<原图面积<1024小目标。固定conf≥.25和IoU≥.5；score0只作可重复忽略匹配，category11不是空间忽略，category0按原版积分图去除覆盖≥.5框。几何无效框排除。逐GT保存ID，逐图保存预测、区域、分数和匹配。
- 可恢复集合：F640未检出、至少一个独立局部视图检出的小GT；另报告Tall融合后仍检出的交集。选中窗几何覆盖与真正检测恢复分别记录；不是GT选区算法或严格最优上界。
- 本批只比较固定片数的选择价值及诊断TP/FP/Recall，不输出官方AP，不完成毫秒预算或实时性证明。缓存预测仅用于准确率配对；实测基础前向和评分耗时只用于开销定位，不伪装端到端延迟。
- 停止：OOM、非有限值、映射/类别/文件错误立即保留失败；上限2小时、10GiB。低阈值/P3-only解释收益、移位等效或P4主要删除有效目标时不自动改公式或加模块。单seed/48图不能作独立泛化或最终否决整个研究方向。

验收：三段CSV连续1–100、末轮checkpoint身份、ZIP校验和证据包；48图完整分母及固定对照、可恢复集合和P4保留/丢弃的证据；结论明确继续验证／暂缓，下一任务单一。运行前登记READY，启动后RUNNING，结束再更新。
