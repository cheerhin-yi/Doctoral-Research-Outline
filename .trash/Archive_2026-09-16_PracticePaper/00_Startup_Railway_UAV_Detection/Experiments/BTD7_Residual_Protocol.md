# BTD7：去重残差与第一片之后的条件检出价值

2026-09-14，分析ID `BTD7-RESIDUAL-20260914-01`。仅复用BTD1 cal48全图/240局部预测、BTD3去重预测及原cal48标注。先核验SHA与既有2720小GT、558可恢复、453已恢复、105残差；任何不一致停止。无模型加载、推理、训练、diag500/test-dev新访问或现有评分调参。

1. 对105残差按既有BTD2定义分为中心未覆盖、覆盖但局部未恢复、所选局部恢复但合并/匹配后消失；后者对照合并前匹配及取消500上限，保留精确原因未分开的情况，不把所有匹配变化称为NMS抑制。
2. 对全部48图/240窗口，固定密度第一片f。基态为原规则NMS(F640+local_f)，对每个j≠f计算NMS(F640+local_f+local_j)及原匹配。分别列新增/丢失小GT、净新增、FP变化、可恢复GT、局部独立重复检出。第一片自身行只作基态，不重复添加。所有新增和损失都保存原标注行号，不能只报告105个残差中的收益。
3. 每图记录固定去重第二片及GT事后最佳第二片的结果；最佳按最终小TP最大、同分窗口索引，不优化FP；另列允许停止于第一片的诊断上界。GT oracle只用于有限窗口池的事后可达性，不是实际方法，不代表达到40ms，不允许输入实际选区。每个残差是否能在固定第一片下经某个第二片合并找回单列，个体可恢复不等于可同时恢复。
4. 推理可得信号只作描述性审计：原粗预测密度、扣除第一片后的新增粗中心、已存low001/P3/candidate分数，以及第一片实际输出在各窗口内的预测中心数量。记录第一片局部预测中不能被同类全图conf≥.25框以IoU≥.5解释的预测中心数（固定预测间关系，不是GT标签）。对全部可选第二片报告同图成对排序一致/相反/同分及零新增粗中心的反例，不拟合、挑阈值或据此运行新选择器。实际信号提取成本未测。
5. cal48已用于开发，全部关联都是探索性。结合既有近邻审查和BTD6超预算结果，结论限定为“存在选择空间/可用新信号证据/新颖性证据”三层；不能由残差或oracle直接认定后两层成立。无多种子或独立场景确认。

资源：CPU NumPy缓存分析，每图前检查总墙钟≤15分钟、RSS≤2GiB、系统可用≥256MiB、磁盘余量≥2GiB；输出≤256MiB（逐图检查）。离散检查不保证帧内峰值。保留失败文件，不覆盖旧运行。交付105例、240窗口、48图、输入/输出清单、守恒核验、结果报告和唯一后续任务。
