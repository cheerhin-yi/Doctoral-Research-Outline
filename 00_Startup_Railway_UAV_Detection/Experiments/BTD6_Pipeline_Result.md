# BTD6：完整流程输出一致，但固定两片仍超参考预算

归档核验：2026-09-14。运行`BTD6-PIPELINE-20260913-01`于9月13日20:39–20:43完成，本次接续只核验和归档，没有重新启动。运行约224.47秒。

**实现校核PASS，时间预算未通过。** 新公共NMS保持全部检测结果，完整流程明显快于旧运行的观测值，但两套固定两片规则仍超出重新校准的F1280相对参考。去重的准确率收益继续有效，不能据此宣称同预算优越或论文创新。

## 1. 已验证内容

- cal48全部48图的两选区流程及F1280中，240次实际NMS输入新旧实现逐行完全相等；预检未计入测速。
- 两规则在diag500各运行3次，共3000次完整计时；全部输出与BTD4存档**逐值完全相同**，最大绝对差0；选区一致、重复输出无差异。协议允许1e-5的跨GPU运行容差，本次未用到容差。
- 全部逐图TP/FP、小目标检出、类别统计及补回/丢失结果保持。小目标23708个，密度检出11129个，去重11402个，Recall46.94%→48.09%；补644、丢371、净增273。这是既有结果的一致性核验，不是新的独立精度证据。
- 权重、val包、cal48/diag500清单及1002份既有记录/预测/汇总身份核验通过；原文件哈希未变。无训练、规则调整、网络修改或test-dev访问。

## 2. 重新测量的完整时间

环境仍为GTX1660SUPER、YOLO11n末轮权重、640/batch1/FP32。数据及完整协议见[预登记](BTD6_Pipeline_Protocol.md)。时间从已解码CPU RGB到最终CPU框，包含转换、全图检测、选区、裁片/局部检测/回映、合并，前后同步GPU；磁盘解码、比对、标注匹配、资源采样和写盘在计时外。

以cal48 F1280每图3次时间的中位数，取48图p95向上整毫秒，得到本次 **T=40ms**。这是相对计算参考，不是已确认的业务截止时间；不沿用旧61ms。

| 指标 | 原密度 | 去重密度 |
|---|---:|---:|
| 实测帧次 | 1500 | 1500 |
| 整帧均值 ms | 51.23 | 51.25 |
| 整帧中位数 ms | 44.44 | 44.22 |
| 整帧p95 ms | 82.63 | 82.90 |
| 超40ms帧次比例 | 98.80% | 99.07% |
| 逐图3次中位数再平均 ms | 50.68 | 50.64 |
| 逐图中位数p95 ms | 82.67 | 82.38 |
| 逐图中位数超40ms比例 | 99.40% | 99.40% |

两规则平均相差约0.01ms，不作为速度差异结论。不能把平均51ms简化成“达到某实时FPS”，也不能把超参考比例解释为铁路业务失效率。

| 平均分项 ms | 原密度 | 去重密度 |
|---|---:|---:|
| 转换与窗口 | 0.54 | 0.55 |
| 全图检测及公共后处理 | 17.38 | 17.34 |
| 选区 | 0.17 | 0.14 |
| 局部裁剪、检测、回映 | 28.17 | 28.21 |
| 最终合并 | 4.96 | 5.01 |

局部处理约占当前平均时间55%，公共合并约10%。因此继续只优化选区代码或最终合并，尚没有证据能让固定两片满足参考预算。不能把两片局部耗时简单除以2，替代尚未运行的一片完整测速。

BTD4的平均整帧约90ms、合并约27ms；本次约51ms和5ms。两次运行之间系统负载不同：BTD4启动时可用内存不足1GiB，本次最低采样可用内存约3.45GiB。故跨运行差值不能全部归因于NMS替换；BTD5交替CPU测试支持算子本身加速，本批则提供更合理实现下的绝对完整耗时证据。F1280在每轮最后计时的顺序设计沿用原协议，可能有系统效应，不宣称严格同时配对或硬实时保证。

## 3. 资源与文件核验

3636次帧前资源采样全部通过预登记阈值：RSS最大1,256,615,936字节（约1.17GiB）、私有内存最大2,257,682,432字节（约2.10GiB）、系统可用内存最小3,702,517,760字节（约3.45GiB）。这是离散采样极值，不是连续峰值；本次未触发停止条件。torch所记录峰值分配143,814,144字节、峰值保留167,772,160字节，不等于进程或整机显存占用。

500份逐图记录、3000条计时、432条校准时间、240条NMS检查、3636条资源样本全部齐全；时间分项和总和、统计守恒、原输出身份及T重算核验通过。来源组Unknown、训练无区域级ignore、开发集反复接触及投稿适用性待核验等限制均未消除。

原始产物在被Git忽略的`11_Datasets/processed/VisDrone/BT1/BTD6-PIPELINE-20260913-01/`，含`summary.json`、`verification.json`、`timings.csv`、`calibration_times.csv`、`resource_samples.csv`、`actual_input_nms_checks.json`、`prior_input_hashes.json`、逐图预测及`output_manifest.json`。代码：[运行入口](run_fast_pipeline.py)、[结果校核](verify_fast_pipeline.py)。原BTD4/BTD5脚本和产物完整保留。

## 4. 对创新判断的影响与下一项唯一任务

本批关闭“是否只是公共后处理太慢”的首轮实现疑问，但没有关闭“有限预算如何分配局部计算”的研究问题。固定两片+简单去重有检出收益，同时存在明显计算代价；不能将其包装成满足同预算的机制。跨尺度弱响应v0.1继续HOLD，不增加模块补救。

**下一项BTD7：cal48去重后残差与条件新增检出价值的缓存审查。** 已有558个可恢复小目标，去重恢复453个，剩105个；核验这105个的身份、未选/局部失败/合并原因，同时统计全部窗口在第一片之后的新增与重复检出，保留损失与反例，避免只看成功残差。

目标是判断：能否从推理可得信号识别值得投入局部计算的区域，还是剩余收益只能靠GT事后挑选。GT只能用于诊断上界，不能用于实际选区；既有近邻覆盖、预算失败和额外信号成本必须共同约束候选判断。先用现有缓存交付信息缺口“支持/不支持/证据不足”，不重训、不复跑已完成诊断、不调弱响应公式，不由缓存宣称新方法满足40ms。不据此自动扩大研究边界。
