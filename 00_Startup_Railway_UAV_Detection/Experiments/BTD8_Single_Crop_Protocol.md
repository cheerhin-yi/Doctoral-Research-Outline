# BTD8：单片动作的完整预算与选择上界

2026-09-14，`BTD8-SINGLE-20260914-01`，先登记后运行。只使用既有cal48；不读diag500/test-dev新数据，不训练、不改变网络或搜索评分。分析分缓存/计时两阶段分别落盘；任一阶段失败保留证据，不重跑已通过阶段。

缓存阶段：核验BTD1预测/记录、cal48图像与标注SHA及BTD7第一片结果。枚举240个既有局部窗口的NMS(全图+该片)，沿用原评价规则、conf=.25/IoU=.5、原图面积<1024和ignore语义。密度第一片固定为粗预测conf≥.25中心最多的窗口，同分索引；GT最佳单片按最终小TP最大，同分索引，不优化FP。另列允许不切片的GT诊断上界。保存每窗口匹配原标注行号、相对密度第一片补回/丢失、FP、最佳窗口与类别统计。GT选择不是方法，不具有可部署性，也不能直接套用密度单片的计时。

计时阶段：固定100轮末EMA、GTX1660SUPER/FP32/batch1，640原像素窗口、原步长512及贴边/短边填充。全图输入640，局部640；每视图conf=.001/max1000，公共CPU NMS沿用BTD5实现，类间独立/IoU>.5/max500。仅将BTD6两片改为密度Top1；F1280只作时间参考，不新增其准确率分析。

先前三张各方法检查通路，再各预热20次。cal48两方法各3次真实前向，共288次计时；图像/重复序号决定K1与F1280交替先后，避免原先参考恒在最后的顺序偏差。每次K1选区和最终输出对照缓存，形状/行序一致、atol=1e-5/rtol=0；记录精确相等数。F1280重复输出按同容差校验。不匹配立即停止，保存失败，不改阈值。

边界为已解码CPU RGB到最终CPU框，包括转换、窗口、全图检测/公共NMS、密度选择、裁片/检测/回映/合并；前后GPU同步。磁盘读取/解码、匹配、缓存比对、资源采样和写盘均在计时外。每次真实计算，禁止缓存充当测速。T仍按cal48 F1280每图3次中位数p95向上取整，不沿用40ms；报告全部帧次与逐图中位数的超参考率、完整时间分布和分项，不声称硬实时或业务截止时间。

资源：每图/每pipeline前采样RSS≤3GiB、私有内存≤4GiB、系统可用≥256MiB，磁盘余量≥5GiB；各阶段总墙钟≤15分钟，输出≤256MiB逐图检查。样本实时追加落盘（计时外），非连续硬上限；OOM/非有限值即停。若失败保留日志，不关闭其他用户程序、不改精度或硬件。原始证据不覆盖。

验收：48图/240单片组合、逐图/逐类小TP和FP、固定简单规则与GT上界差及损失；288条真实计时、输出一致性、重新校准T、资源记录/快照/SHA；结论区分动作预算与选择空间。若单片仍明显超参考或GT上界接近简单选择，暂缓当前配置下区域排序作为唯一创新，先给出研究问题调整依据；不自动换网络/模块/窗口。若存在空间，也只开放针对性差异审查，不等于新信号、独立泛化或新颖性通过。
