# BTD5公共NMS等价性与计时协议

2026-09-13；分析ID `BTD5-NMS-20260913-01`。先登记再运行，仅CPU数组/已有cal48缓存；不加载检测器、不推理、不改选区、不用diag500/test-dev，不改历史脚本或预测。

参考为`diagnose_bt1.nms`：float64、按原分数稳定排序、类别独立、IoU严格>.5抑制、最终全类别cap500。待检实现为现有torchvision CPU编译算子：逐类float64框，用唯一递减秩分数控制同分顺序，保留原始预测行，最终按全局稳定秩取500。不用类别坐标偏移或给真实分数加epsilon；微小/退化框及非有限等异常输入回到参考实现以保留分母下限行为。该候选只有工程意义。

等价性：空输入、同分跨类/同类、IoU=.5及两侧邻值、链式抑制、cap边界、退化/极小框、非整数cap回退，100组seed20260913随机/重复/同分输入；逐数组`array_equal`，不接受近似坐标或指标相同替代精确行一致。

真实缓存：核验BTD1输入SHA，48图全图、所有240局部、密度/去重各48组合、Tall48组合，共432组；保留原视图顺序及预测索引。先验证全部，再预热每组两个实现各1次，交替先后执行，每实现每组重复11次。时间包含数组整理、张量包装/秩生成、逐类NMS及最终排序，不含文件加载/缓存解压；不调用GPU。报告逐组合中位/均值/p95、数据规模、速度比及每组输出版次一致性。

失败：任何有效域输出不一致即停止准入，不以平均指标掩盖；不自动改阈值或同分约定。上限10分钟/1GiB，内存负载记录。通过后只将新实现标为可用于后续共用流程复测，不改写BTD4，也不以孤立NMS速度外推完整FPS。后续整帧复测必须同步重校F1280参考T。
