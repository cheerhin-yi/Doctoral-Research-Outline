# 学习笔记1：从基本YOLO到论文级目标检测

_统一记录方法见[项目学习笔记统一记录方法](../../00_Overview/Learning_Note_Method.md)。以下内容必须掌握，其他内容沿知识链按需补充。_

---

## 🎯 必须掌握

1. **完整检测链及训练/推理分支**
   - 复述要求：闭卷讲清输入、Backbone、Neck和Head的数据流，以及训练和推理怎样分叉。
   - 验证方式：独立画图并标出各节点输入、处理和输出。
2. **预测、监督与评价的连接关系**
   - 复述要求：讲清标签匹配、损失、解码、NMS、IoU、Precision、Recall、AP和mAP各自所在位置。
   - 验证方式：完成一组IoU、Precision、Recall和NMS手算，并解释AP与mAP的区别。
3. **已知目标检测错误的证据链**
   - 复述要求：区分漏检、误检、定位偏差和重复框可能来自哪些环节。
   - 验证方式：用至少两个案例写出“错误→证据→候选环节→修正或不修正”的判断。

## 🔗 主题知识链

> 图像 → Backbone → Neck → Head → 训练损失或推理解码/NMS → 指标 → 错误分析 → Paper 1已知检测决策

## ✍️ 灵活记录原则

沿上面的链条在对应章节补充“我的理解、证据、例子、疑问和修正”。可使用文字、要点、手写图链接、Mermaid、公式、代码输出或源码位置，不要求重复填写大表，也不要求一次写完。

## 你的起点

你已经学习卷积和YOLO基本概念，因此跳过卷积定义、经典CNN历史和YOLO版本年表。现在需要建立“结构—训练—指标—错误—论文实验”的完整链。

## 为什么服务Paper 1

Paper 1所有结论都建立在可靠YOLO基线上。如果不会解释Head输出、损失、标签匹配、NMS和指标，就无法判断改进来自模型、训练设置、阈值还是数据泄漏。该笔记直接支撑已知危险检测，也是未知候选和风险告警的输入。

## 📚 沿知识链补充

### 1. 完整数据流

需要能够解释：

```text
Image[B,3,H,W]
→ Backbone提取多层特征
→ Neck融合不同尺度
→ Detection Head输出分类与框参数
→ 解码候选框
→ 置信度筛选与NMS
→ Boxes + Classes + Scores
```

学习任务：

- [ ] 画出一张完整结构图；
- [ ] 标出每层特征图分辨率和通道的变化；
- [ ] 解释浅层高分辨率特征为什么对小目标重要；
- [ ] 说明训练阶段和推理阶段分别在哪一步不同。

### 2. Backbone、Neck与Head

- Backbone：提取局部纹理到高级语义；
- Neck：通过多尺度融合兼顾小、中、大目标；
- Head：预测类别和位置；
- 重点不是背模块名称，而是能判断某个失败是否真由该部分造成。

必须回答：

1. 小目标消失可能发生在哪些下采样位置？
2. 只增加更深网络为什么可能不改善小目标？
3. 提高输入分辨率与修改多尺度特征的代价有何不同？

### 3. 训练目标

掌握以下直觉和最小公式：

- IoU：预测框和真值框的重叠比例；
- 分类损失：预测类别与标签的差异；
- 定位损失：框的位置和尺度误差；
- 标签匹配：决定哪些预测负责哪些真值；
- 类别不平衡：大量背景和少量危险类别会怎样影响训练。

学习输出：写一张表，列出“分类错误、漏检、重复框、定位偏差”分别可能与哪些训练环节有关。

### 4. 推理与NMS

必须理解置信度阈值和IoU阈值会同时改变Precision、Recall和每图候选数。

手工练习：给出3个重叠框、类别分数和阈值，逐步执行NMS，说明哪个框被删除以及为什么。

### 5. 评价指标

必须掌握：

预测框 $B_p$ 与真值框 $B_g$ 的交并比为：

$$
\operatorname{IoU}(B_p,B_g)
=
\frac{|B_p\cap B_g|}{|B_p\cup B_g|}
$$

Precision、Recall与F1为：

$$
\operatorname{Precision}=\frac{TP}{TP+FP},
\qquad
\operatorname{Recall}=\frac{TP}{TP+FN}
$$

$$
F_1
=
\frac{2\,\operatorname{Precision}\,\operatorname{Recall}}
{\operatorname{Precision}+\operatorname{Recall}}
$$

在类别 $c$ 和IoU阈值 $\tau$ 下，AP可写为PR曲线下面积：

$$
\operatorname{AP}_{c,\tau}
=
\int_{0}^{1} p_{c,\tau}(r)\,\mathrm{d}r
$$

对 $C$ 个类别取平均：

$$
\operatorname{mAP}_{\tau}
=
\frac{1}{C}\sum_{c=1}^{C}\operatorname{AP}_{c,\tau}
$$

其中，$TP$、$FP$和$FN$分别表示真正例、假正例和假负例；$p(r)$表示以Recall为横轴时的Precision。实际实现通常采用离散插值而不是直接计算连续积分。

还要理解PR曲线、mAP50、mAP50–95、逐类AP和尺寸分层Recall。mAP50–95通常表示在IoU阈值$0.50,0.55,\ldots,0.95$上计算mAP后再取平均。

Paper 1优先级：

1. 危险目标Recall；
2. 固定告警预算下的危险Recall；
3. 小目标和不同高度的分层Recall；
4. mAP50–95；
5. FPS、时延、参数和显存。

不能只报告mAP50，因为它对定位质量要求较低，也可能掩盖危险类别漏检。

### 6. 铁路UAV错误分类

对每个错误至少记录：

- 目标尺寸：small/medium/large；
- 飞行高度或像素尺寸；
- 遮挡程度；
- 背景类型；
- 类别；
- 错误类型：漏检、误检、分类错、定位差、重复框；
- 是否为高风险目标。

错误分析的目的不是展示更多图，而是决定是否存在一个值得测试的最小改动。

## 最小练习

在官方示例或极小公开样例上完成：

1. 运行一次YOLO推理；
2. 保存原始预测框、类别和分数；
3. 改变置信度阈值，记录候选数和漏检变化；
4. 手工检查10张图，建立错误表；
5. 不做正式训练，不声称性能结果。

## 必须留下的笔记

- [ ] YOLO完整结构图；
- [ ] 训练与推理差异表；
- [ ] IoU/Precision/Recall手算；
- [ ] NMS手算；
- [ ] mAP50与mAP50–95区别；
- [ ] 铁路UAV错误分类表；
- [ ] “一个改动如何由错误证据触发”的例子。

## 暂时不学

- YOLO所有历史版本；
- 同时修改Backbone、Neck、Head和Loss；
- 大量注意力模块；
- RT-DETR内部细节；
- TensorRT部署和边缘硬件优化。

## ✍️ 沿知识链记录（以下格式均可选）

### 1. 学习会话记录

| 日期 | 本次范围 | 使用资料 | 用时 | 完成证据 |
|---|---|---|---:|---|
|  |  |  |  |  |

### 2. 核心概念笔记

| 概念 | 我的解释 | 铁路UAV例子 | 适用条件/失败情况 |
|---|---|---|---|
| Backbone |  |  |  |
| Neck与多尺度 |  |  |  |
| Detection Head |  |  |  |
| 标签匹配与损失 |  |  |  |
| NMS |  |  |  |
| Precision/Recall/AP |  |  |  |

### 3. 图、公式与手算

- YOLO结构图或文件链接：
- 张量尺寸变化：
- IoU/Precision/Recall手算：
- NMS手算：
- mAP50与mAP50–95的区别：

### 4. 错误分析记录

| 样例/现象 | 错误类型 | 可能原因 | 需要什么证据才能判断 |
|---|---|---|---|
|  |  |  |  |

### 5. 疑问与理解修正

| 原来的理解或疑问 | 查到的证据 | 修正后的理解 | 是否解决 |
|---|---|---|---|
|  |  |  |  | 否 |

### 6. 闭卷自测与总结

1. 为什么更大的YOLO不一定更适合铁路无人机？
   我的回答：
2. 为什么提高分辨率不能直接证明网络结构创新？
   我的回答：
3. Precision提高而Recall下降时，巡检系统可能发生什么？
   我的回答：
4. 怎样公平比较YOLO11n/s/m？
   我的回答：
5. 哪类错误才能合理触发一个结构改动？
   我的回答：

- 不看资料写出的三句话总结：
- 我仍然不清楚的环节：
- 本轮是否通过：否
- 下一次只做什么：
