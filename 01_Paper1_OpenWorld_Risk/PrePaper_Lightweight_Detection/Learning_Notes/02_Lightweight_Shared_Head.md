# 02 轻量共享检测头学习笔记

## 学习目标

理解检测头参数共享和深度可分离卷积，能够画出并实现一个不依赖复杂算子的LSM-Head。

## 学习会话记录

| 日期 | 资料 | 本次问题 | 留下的证据 | 未解决问题 |
|---|---|---|---|---|
|  |  |  |  |  |

## 1. 检测头基础

- 分类分支输出什么：
- 回归分支输出什么：
- 多尺度独立检测头为什么参数更多：
- 跨尺度共享参数的前提是什么：
- 为什么仍需保留尺度独立输出层：

## 2. 参数量手算

设输入通道为 $C_{\mathrm{in}}$、输出通道为 $C_{\mathrm{out}}$、卷积核为 $K\times K$。以下公式暂不计bias。

普通卷积参数量：

$$
P_{\mathrm{conv}}
=
K^2C_{\mathrm{in}}C_{\mathrm{out}}
$$

深度卷积和逐点卷积参数量：

$$
P_{\mathrm{dw}}=K^2C_{\mathrm{in}},
\qquad
P_{\mathrm{pw}}=C_{\mathrm{in}}C_{\mathrm{out}}
$$

深度可分离卷积总参数量及相对节省比例：

$$
P_{\mathrm{ds}}
=
K^2C_{\mathrm{in}}+C_{\mathrm{in}}C_{\mathrm{out}}
$$

$$
R_{\mathrm{save}}
=
1-\frac{P_{\mathrm{ds}}}{P_{\mathrm{conv}}}
$$

若三个尺度各自使用完整干路，检测头参数量为：

$$
P_{\mathrm{independent}}
=
\sum_{l\in\{2,3,4\}}
\left(P_{\mathrm{stem}}^{(l)}+P_{\mathrm{out}}^{(l)}\right)
$$

若共享干路、保留尺度独立输出层，则为：

$$
P_{\mathrm{shared}}
=
P_{\mathrm{stem}}
+
\sum_{l\in\{2,3,4\}}P_{\mathrm{out}}^{(l)}
$$

共享结构是否真正提高速度仍需真实延迟实验，不能仅由参数量公式推出。

选择一组实际通道数完成计算：

| 配置 | 普通卷积参数量 | 深度可分离卷积参数量 | 节省比例 |
|---|---:|---:|---:|
|  |  |  |  |

## 3. LSM-Head结构草图

必须标出：P2/P3/P4输入、通道对齐、共享卷积干路、分类输出和回归输出。

> 在此插入结构图或本地文件链接：

## 4. 实现前检查

- [ ] 三个输入特征通道是否一致；
- [ ] 共享模块是否真的引用同一组参数；
- [ ] 每个尺度输出形状是否符合检测器要求；
- [ ] 使用的算子能否导出ONNX；
- [ ] 除检测头外是否意外改动了Neck、Loss或增强；
- [ ] B2和M之间是否只有“共享”一个变量。

## 5. 最强反对意见

- 参数共享可能导致的精度损失：
- 深度可分离卷积GFLOPs低但实际不快的原因：
- P2增加的访存成本：
- 如果共享头失败，为什么不继续增加注意力模块：

## 6. 闭卷自测

1. “参数少”为什么不等于“延迟低”？
2. B1、B2和M分别隔离什么因素？
3. 这个方法为什么仍可视为一个检测头改动？

三句话总结：

1. 
2. 
3. 
