# 03 速度评价与简单部署学习笔记

## 学习目标

能够区分理论复杂度和真实速度，建立一个可复核的batch=1计时协议。真实无人机部署不是必修内容。

## 学习会话记录

| 日期 | 资料 | 本次问题 | 留下的证据 | 未解决问题 |
|---|---|---|---|---|
|  |  |  |  |  |

## 1. 指标区别

| 指标 | 自己的解释 | 受什么影响 | 本论文怎样测 |
|---|---|---|---|
| 参数量 |  |  |  |
| GFLOPs |  |  |  |
| 模型大小 |  |  |  |
| 峰值显存 |  |  |  |
| 纯推理延迟 |  |  |  |
| 端到端延迟 |  |  |  |
| FPS |  |  |  |

## 2. 固定计时协议

- 硬件与功耗模式：
- 软件版本：
- 输入尺寸：
- batch size：1
- 精度：FP32 / FP16：
- 预热次数：
- 正式计时次数：
- 是否包含预处理：
- 是否包含NMS：
- 延迟统计：均值 / 中位数 / P95：

若顺序处理、batch为1，且单帧平均延迟为 $t_{\mathrm{ms}}$ 毫秒，则理论FPS为：

$$
\operatorname{FPS}=\frac{1000}{t_{\mathrm{ms}}}
$$

相对基线延迟 $t_{\mathrm{base}}$，新模型延迟 $t_{\mathrm{new}}$ 的加速比与延迟下降比例为：

$$
S_{\mathrm{latency}}
=
\frac{t_{\mathrm{base}}}{t_{\mathrm{new}}}
$$

$$
R_{\mathrm{latency}}
=
\frac{t_{\mathrm{base}}-t_{\mathrm{new}}}{t_{\mathrm{base}}}\times100\%
$$

只有计时范围一致时，这些结果才可比较。若包含预处理和NMS，应明确写成端到端延迟。

## 3. 最小部署层级

按条件从上到下完成，不强制全部做：

1. PyTorch固定GPU延迟；
2. ONNX Runtime延迟；
3. TensorRT FP16延迟；
4. 有现成设备时测试Jetson。

不需要真实无人机、飞控连接、通信链路或航时实验。

## 4. 公平性检查

- [ ] 所有模型使用相同输入尺寸和batch；
- [ ] 所有模型使用相同精度和计时范围；
- [ ] 预热和正式计时次数相同；
- [ ] 不把训练吞吐量当作推理速度；
- [ ] 不用台式GPU速度宣称“机载实时”；
- [ ] GFLOPs与真实延迟分别报告。

## 5. 论文结果模板

| 模型 | Params | GFLOPs | Model Size | Latency | FPS | AP_small |
|---|---:|---:|---:|---:|---:|---:|
| B0 |  |  |  |  |  |  |
| B1 |  |  |  |  |  |  |
| B2 |  |  |  |  |  |  |
| M |  |  |  |  |  |  |

## 6. 闭卷自测

1. 为什么深度可分离卷积可能在某些设备上不快？
2. 怎样避免把预处理或NMS差异误认为模型加速？
3. 没有Jetson时，论文可以诚实地得出什么结论？

三句话总结：

1. 
2. 
3. 
