# 学习笔记2：PyTorch科研能力与可复现性

_统一记录方法见[项目学习笔记统一记录方法](../../00_Overview/Learning_Note_Method.md)。以下内容必须掌握，其他内容沿知识链按需补充。_

---

## 🎯 必须掌握

1. **一次PyTorch训练与验证的数据和计算链**
   - 复述要求：讲清Tensor、标签、batch、DataLoader、forward、loss、backward、optimizer和validation的连接关系。
   - 验证方式：对一段最小训练循环逐行标注张量形状、梯度发生位置及train/eval差异。
2. **实验可复现记录与恢复**
   - 复述要求：说明seed、环境、配置、AMP、checkpoint、日志和Run ID各自控制什么，哪些仍不能保证完全确定性。
   - 验证方式：列出一个运行的最小复现清单，并证明checkpoint能够恢复模型和优化器状态。
3. **数据隔离与泄漏识别**
   - 复述要求：讲清训练、验证和测试集的职责，以及同源帧或增强副本为何可能泄漏。
   - 验证方式：给定一个划分案例，指出泄漏路径并写出不泄漏的重划分规则。

## 🔗 主题知识链

> 数据与标签 → Dataset/DataLoader → forward → loss → backward/optimizer → validation → checkpoint与日志

## ✍️ 灵活记录原则

沿上面的链条在对应章节补充“我的理解、证据、例子、疑问和修正”。可使用文字、要点、手写图链接、Mermaid、公式、代码输出或源码位置，不要求重复填写大表，也不要求一次写完。

## 为什么服务本练手论文

会调用`train`命令不等于能做研究。本论文需要判断张量是否正确、数据是否泄漏、训练和验证模式是否切换、B0/B1/B2/M配置是否一致、结果是否能复现。这些能力也可在Paper 1中独立学习和记录。

## 📚 沿知识链补充

### 1. Tensor与形状

- B/C/H/W分别表示什么；
- dtype、device和归一化范围；
- 图像、框、类别标签在批量中的组织方式；
- shape不一致为何可能不报错但产生错误结果。

任务：写出一批4张 $640\times640$ RGB图像的输入形状，并说明检测标签为何不能简单堆成固定二维数组。

### 2. Dataset与DataLoader

需要掌握：

- `__len__`和`__getitem__`；
- transform只作用于训练集还是所有集合；
- shuffle、batch、worker和collate；
- 同一视频相邻帧为什么不能随机分到训练和测试；
- 原图、增强图和裁剪图如何防止跨集合。

### 3. forward、loss与autograd

最小训练流程：

```text
model.train()
for batch:
    optimizer.zero_grad()
    prediction = model(image)
    loss = criterion(prediction, target)
    loss.backward()
    optimizer.step()
```

需要解释每一行做什么，以及验证时为什么不执行反向传播。

### 4. train与eval

掌握：

- `model.train()`与`model.eval()`；
- Dropout和BatchNorm行为差异；
- `torch.no_grad()`的作用；
- 验证集用于选择模型，测试集只用于最终评价。

### 5. optimizer与学习率

只需掌握SGD/AdamW的基本区别、学习率、weight decay、warmup和scheduler的作用。当前不进行大规模超参数搜索。

必须回答：如果方法A调了20组超参数、基线只用默认值，比较是否公平？

### 6. AMP、checkpoint与恢复

- mixed precision为何节省显存、何时可能数值不稳定；
- checkpoint至少包含模型、优化器、epoch和配置；
- best与last checkpoint的区别；
- 恢复训练和加载推理权重的区别。

### 7. 随机性与日志

每次正式运行至少记录：

- Run ID；
- Git提交或代码版本；
- 数据版本与split；
- 配置；
- 随机种子；
- 环境和硬件；
- 开始/结束时间；
- 原始指标和模型权重路径；
- 是否成功及异常。

核心结论受随机性影响时使用3个种子；早期排错可以单种子，但不能据此写最终结论。

## 最小练习

不要用项目正式数据。选择一个极小分类示例或框架自带示例：

1. 检查一个batch的shape、dtype、device和标签；
2. 写出train/val伪代码；
3. 固定seed运行两次，比较输出；
4. 保存并恢复checkpoint；
5. 故意漏掉`eval()`，观察并解释潜在问题。

## 练手论文直接应用

- B0/B1/B2/M公平比较；
- VisDrone官方划分与增强泄漏检查；
- 置信度和NMS阈值只在验证集选择；
- 每个消融固定训练预算；
- 实验表可回链到运行配置。

## 必须留下的笔记

- [ ] 一张数据加载流程图；
- [ ] train/val伪代码；
- [ ] 一份checkpoint字段清单；
- [ ] 一份练手论文最小运行记录模板；
- [ ] 三个数据泄漏例子；
- [ ] 固定seed不能保证完全确定性的原因。

## 暂时不学

- 分布式训练；
- 自定义CUDA算子；
- 大规模超参数平台；
- 模型量化和部署编译；
- 复杂训练框架重构。

## ✍️ 沿知识链记录（以下格式均可选）

### 1. 学习会话记录

| 日期 | 本次范围 | 使用资料/代码 | 用时 | 完成证据 |
|---|---|---|---:|---|
|  |  |  |  |  |

### 2. 核心概念笔记

| 概念 | 我的解释 | 常见错误 | 练手论文中的作用 |
|---|---|---|---|
| Tensor与B/C/H/W |  |  |  |
| Dataset/DataLoader |  |  |  |
| forward/loss/autograd |  |  |  |
| train/eval/no_grad |  |  |  |
| optimizer与学习率 |  |  |  |
| AMP与checkpoint |  |  |  |
| seed与日志 |  |  |  |

### 3. 代码与流程记录

- train/val伪代码：
- batch的shape、dtype和device：
- checkpoint包含字段：
- 最小练习代码/输出位置：
- 两次固定seed运行的差异：

### 4. 数据泄漏与复现清单

| 风险 | 如何发现 | 如何避免 | 当前是否理解 |
|---|---|---|---|
| 相邻帧跨集合 |  |  | 否 |
| 原图/增强图跨集合 |  |  | 否 |
| 测试集选择阈值 |  |  | 否 |

### 5. 疑问与理解修正

| 原来的理解或疑问 | 调试/资料证据 | 修正后的理解 | 是否解决 |
|---|---|---|---|
|  |  |  |  | 否 |

### 6. 闭卷自测与总结

1. 训练集、验证集、测试集分别能做什么？
   我的回答：
2. `train()`、`eval()`和`no_grad()`有什么区别？
   我的回答：
3. 为什么视频相邻帧随机划分会虚高性能？
   我的回答：
4. 怎样排除改进只来自更长训练或更大输入？
   我的回答：
5. 哪些信息缺失会使一次实验无法复现？
   我的回答：

- 不看资料写出的三句话总结：
- 我需要继续补的代码能力：
- 本轮是否通过：否
- 下一次只做什么：
