# N01 PyTorch科研能力

**状态**：LIMITED  
**当前项目用途**：只支撑前置论文A的校准脚本运行、复现和结果保存。

## 当前只学

- 读取预测CSV/JSON及检查字段、类型和缺失值；
- `train/eval`差异及禁止在测试集拟合参数；
- 固定随机种子、保存配置、环境和输出；
- 能调试一个简单校准训练/推理循环。

## 当前不学

YOLO结构修改、复杂训练循环、分布式训练、AMP优化、模型部署和性能调优。已有库能完成校准时，不重复造轮子。

## 最小练习

读取一份小型预测表，按`scene_id`划分校准/测试，固定种子拟合一个简单校准器，并再次运行得到一致输出。

## 完成证据

- 可重复运行命令或Notebook；
- 环境和种子记录；
- 输入字段检查和输出指标文件。

Paper 1恢复后，再为YOLO训练和调试扩展本模块。
