# N04 铁路几何

**状态**：LIMITED  
**当前职责**：A使用归一化轨道位置；B使用固定双视角轨道坐标。

## 前置论文A只学

- 图像坐标、轨道中心/边界和局部轨道宽度；
- `d_norm=(x_object-c(y))/w(y)`的含义与有效条件；
- 为什么归一化量不是物理距离；
- 轨道不可见、弯道、道岔和分割误差下的失效标志。

## 前置论文B再学

- 两个固定视角映射到同一轨道相对坐标；
- 对象对应、坐标差和一致性阈值；
- 对齐失败和冲突标志。

## 当前删除

标准轨距物理测量、完整相机标定、RTK/GCP、BEV、Homography系统比较和误差传播。这些属于M1。

## 完成证据

公式推导、输入单位、有效/无效样例以及归一化位置生成记录。
