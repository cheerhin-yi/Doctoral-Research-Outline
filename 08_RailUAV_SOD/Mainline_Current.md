# 主线当前执行页：RailUAV-SOD（A）

更新：2026-09-24（Asia/Shanghai）。  
效力：预备叙述；与 `Current_Stage` 冲突时以 **Current_Stage** 为准。

## 1. 资源边界（已确认口径）

- 当前算力：双 4090 等可用资源仍优先服务 **P0_EI**，本包 IDLE 时不抢占。
- **尚不具备（占位禁止）**：已发布的本项目 UAV 铁路小部件×真实缺陷正式集。不得当作已有结果写入投稿稿。
- 未来可 UAV 采线：作为方向前提（见 AB 判断），**不是**今日开工单。

## 2. 近 / 中 / 远

| 时段 | 做什么 | 不做 |
|---|---|---|
| **近**（P0_EI 收口前） | 只维护本预备包：文献矩阵、提纲、阶段门 IDLE | 不采集、不训练、不开主 Run |
| **中**（Current_Stage 再授权后） | A0→A1：划界审计、taxonomy、采集协议 | 不并行抢 B 的告警主实验 |
| **远**（数据可发布后） | A3–A5：效率审计、合成曲线、基线、Sci Data 稿 | 不把告警预算写进 A 主张 |

## 3. 当前研究问题（预备效力）

UAV 视角下，铁路小部件×真实缺陷基准如何公开可复用？双引擎 OVD 预标注的人时效率与合成 fidelity–utility 如何可审计？

## 4. 主张（最多两项）

见 `Research_Plan.md`：A-C1 / A-C2 · **PROPOSED · IDLE**。

## 5. 成功标准（本预备阶段）

1. 目录架构与 Practice 对齐；STATUS 明示 PREP/IDLE。
2. 与 B 独立边界可自检（删引用后主表仍完整）。
3. 不改变 `Current_Stage` 唯一 ACTIVE=P0_EI。
