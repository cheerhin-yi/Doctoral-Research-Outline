# 学习笔记8：航拍 SOD 封闭集基线与指标

_沿「基线是压力测试数据，不是你的新发明」建立知识链：同协议表轴 → 小目标/稀缺指标 → 合规 Validation 句。YOLO/RT-DETR = 基线工具，非本篇贡献。_

---

统一原则见[项目学习笔记统一记录方法](../../00_Overview/Learning_Note_Method.md)。  
关卡：**L3**。检查基准见 [`../Learning_Check_Baseline.md`](../Learning_Check_Baseline.md) §2.8。先修：`03` 忽略区/D5、`04` 原图尺度、`05` Validation 角色。  
**禁止：** 把 B 的告警预算/危险召回写进本篇主表。未跑训练也可过概念关。

## 🎯 必须掌握

1. **同协议基线表轴**
   - 复述要求：说明 Technical Validation 里同协议基线表最少固定哪些轴。
   - 验证方式：草拟空表表头。
2. **小目标/稀缺指标与 D0–D5、忽略区对齐**
   - 复述要求：把指标口径接回原图像素协议与 D5/忽略规则；不照抄 VisDrone 当铁路标准。
   - 验证方式：接口意识；稀缺 vs 合成并列披露。
3. **基线证明数据可用 ≠ 方法刷 SOTA**
   - 复述要求：区分工具角色与 novelty；会写合规/违规 Validation 句。
   - 验证方式：合规/违规句各一句。
4. **无 B 告警指标自检**
   - 复述要求：删光 budget/alarm 句后基线表仍完整。
   - 验证方式：自检句；出现告警主表 → REPEAT。

## 🔗 主题知识链

按下面的连接关系组织学习：

> 冻结 taxonomy 与 split 意识（未授权不跑主 Run） → 选封闭集检测器族 → 同协议训练/评价轴 → 主指标 + 稀缺 Recall + 忽略/D5 → 空基线表表头 + 合规 Validation 句 → Technical Validation 可训练性证据柱（仍不授权训练）

学习时始终追问四件事：

1. 这一环节接收什么？
2. 它做了什么？
3. 它输出什么给下一环节？
4. 如果这里出错，最终会出现什么可观察现象？

## ✍️ 灵活学习记录

### 基线会话

- 本次只学：
- 使用资料及版本：
- 新增或修正的理解：
- 留下的证据或问题：

## 📚 按结构填写知识链

**术语：** 封闭集检测｜同协议｜mAP｜稀缺类召回｜NMS｜imgsz｜原图尺度协议

### 1. 为什么数据文也要跑 YOLO / RT-DETR

审稿人要看到：别人下载你的数据后，用常见检测器按你的协议能得到合理分数；失败模式可解释。换成更新的检测器可以，但**换协议却不说明**会使表不可比。基线模型本身通常**不算** A 的 novelty。

- **追问/要点（填写）：**
  - 它接收什么：
  - 它做什么：
  - 它输出什么：
  - 失败现象：
  - 对 A 的影响：
  - 我的证据/疑问：

### 2. 同协议表轴与 D0–D5 对齐

最少固定并披露：模型名与权重来源｜输入尺寸与是否 letterbox｜增强是否用于 val/test（通常否）｜置信度阈值与 NMS｜类别映射（part×defect → 训练类）｜split 哈希 / 场景隔离｜硬件与随机种子｜忽略区与 D5 是否计入。

| 内容 | 建议 |
|---|---|
| D0 | 对照；是否单独训练负例按协议 |
| D1–D4 | 主表候选 |
| D5 | 不进主 mAP 或单独质量列 |
| 忽略区 | 不计入该区 TP/FP |

小目标「small」阈值要按本项目 GSD/像素协议冻结；**禁止**直接宣称「遵循 VisDrone small 定义故铁路科学」而不说明域差异。未授权前：只学「表轴」，**不跑主 Run**。

- **追问/要点（填写）：**
  - 它接收什么：
  - 它做什么：
  - 它输出什么：
  - 失败现象：
  - 对 A 的影响：
  - 我的证据/疑问：

### 3. 合规句、失败边界与禁止说法

- **合规例：** 「Under a fixed split and ignore-region protocol, YOLO11 and RT-DETR provide closed-set baselines confirming the benchmark is trainable.」
- **违规例：** 「We propose a novel detector that significantly outperforms SOTA and improves alarm recall under budget.」——前半抢方法贡献，后半属 B。

失败边界例意识：强阴影弹条、砟掩埋、远端尺度断崖、板缝假裂纹——各给图例，证明你理解域，而不是只报总分。稀缺真实 Recall 很低、合成曲线却很高时：并列披露，收缩「真实缺陷基准已充足」措辞。

| 禁止 | 改法 |
|---|---|
| 基线模型当 novelty | 写明工具角色 |
| 改忽略区抬 mAP 不披露 | 冻结并哈希协议 |
| 只报总体 mAP 藏稀缺类崩盘 | 并列稀缺 Recall |
| 告警 AUPRC 进 A 主表 | 删；留给 B |

Technical Validation 最少让读者复现三件事：（1）数据划分与忽略规则；（2）训练/推理超参与后处理；（3）主指标计算脚本或足够细节。

- **追问/要点（填写）：**
  - 它接收什么：
  - 它做什么：
  - 它输出什么：
  - 失败现象：
  - 对 A 的影响：
  - 我的证据/疑问：

## ✅ 学完知识链后回答的必学问题

1. 为什么基线模型本身通常不算 A 的 novelty？

   我的回答：

2. 忽略区定义改变却不披露时，mAP 比较会发生什么？

   我的回答：

3. Technical Validation 里最少要让读者复现哪三件事？

   我的回答：

4. 草拟空基线表表头（列名即可）。

   我的回答：

5. 请闭卷串讲：冻结协议 → 同协议表轴 → 指标/忽略 → 合规句 → 非告警。

   我的回答：

## 🧠 易混点闭卷自测

1. Learning PASSED 是否授权正式训练？
2. VisDrone small 定义能否直接当铁路标准？
3. 告警 AUPRC 能否进 A 主表？

<details>
<summary><strong>自学核对提示（不是标准答案）</strong></summary>

1. 否。  
2. 否；须按本项目 GSD/像素协议冻结并说明域差异。  
3. 否；属 B。

</details>

## 🎯 本笔记的最小完成证据

- [ ] 空基线表表头一行
- [ ] 合规/违规 Validation 句各一句
- [ ] 稀缺真实 Recall vs 合成曲线并列披露意识
- [ ] 「Learning PASSED ≠ 授权训练」句
- [ ] 必学问题均已回答或标「暂不能回答」
- [ ] 能闭卷串讲知识链

## 🚫 当前边界

- 不正式训练、不写 Run ID、不把占位数字填成已跑分数
- 不写告警预算实验（属 B）
- ACTIVE 仍为 **P0_EI**；学习通过 ≠ 训练授权

## 📋 完成评审

- 当前状态：
- 我最有把握的环节：
- 我仍然无法连接的环节：
- 本轮形成的文件或证据：
- 是否通过本块过关：
- 通过后下一任务：`09` 主线 A 知识链
