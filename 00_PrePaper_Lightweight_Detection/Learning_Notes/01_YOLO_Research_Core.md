# 学习笔记1：YOLO研究核心

_沿YOLO结构建立知识链；先记录理解，再集中回答必学问题。_

---

统一原则见[项目学习笔记统一记录方法](../../00_Overview/Learning_Note_Method.md)。
## 🎯 必须掌握

1. **完整检测链及训练/推理分支**
   - 复述要求：闭卷讲清输入如何经过Backbone、Neck和Head，并分别进入训练与推理分支。
   - 验证方式：独立画出结构图，为每个节点标出输入、处理和输出。
2. **预测成为损失或最终检测结果的机制**
   - 复述要求：讲清标签分配、损失计算、框解码和NMS分别解决什么问题及其先后关系。
   - 验证方式：完成一组IoU、Precision、Recall和NMS手算，并解释AP与mAP的评价范围。
3. **从错误证据回到结构环节**
   - 复述要求：区分漏检、误检、定位偏差和重复框，并说明可能关联的环节。
   - 验证方式：针对至少两个错误案例写出“现象→证据→候选原因→是否改结构”的判断链。

## 🔗 主题知识链

按下面的连接关系组织学习：

> 输入与预处理 → Backbone → 多尺度特征 → Neck → Detection Head → 训练分支 / 推理分支 → 评价指标 → 错误证据 → 是否需要结构改动

学习时始终追问四件事：

1. 这一环节接收什么？
2. 它做了什么？
3. 它输出什么给下一环节？
4. 如果这里出错，最终会出现什么可观察现象？

## ✍️ 灵活学习记录

不要维护复杂表格。每次学习复制下面四行即可：

### YOLO8

- 本次只学：YOLO8
- 使用资料及版本：[[万字详解YOLOv8网络结构]]
- 新增或修正的理解：
- 留下的证据或问题：

## 📚 按结构填写知识链

### 1. 输入与预处理

- 我看到的原始输入是什么：图形尺寸为`[640, 640, 3]`
- 进入模型前发生了什么：会进行反转、对称、模糊等等的预处理，可以增大数据量和防止过拟合
- 输入张量或尺寸怎样表示：`[64, 640, 640, 3]`，64是批次
- 哪些处理只在训练出现，哪些也在推理出现：BackBone、Neck、Head都会出现，区别在于训练会在分类和瞄框时做损失函数
- 这一环节可能制造的数据偏差或泄漏：不清楚

### 2. Backbone 特征提取
基本组成是使用一系列卷积和反卷积层。基本使用如下。
1. Cond2d卷积[[3 模型构建]]
2. BatchNorm2d层：归一化[[6 正则化Regularization]]
3. SiLU激活函数:关键特性是它允许平滑的梯度
4. **瓶颈块Bottleneck Block**
	1. 基本思想：基本就是残差块[[00_ResNet_Core]]，每个Bottleneck Block也是**1×1 → 3×3 → 1×1**卷积，输出一个映射
	2. 本质：也可以是残差网络，每个Bottleneck Block是**3×3 → 3×3** 卷积，通过两个卷积把维度配置到想要的某个值，在配置项里面可以应用经典的**分组卷积、深度可分离卷积**等等，也可以配置最后的输出是否进行跳跃链接（残差配置）
### 2.1 卷积块（CBS）
整个YOLO网络里面所涉及的卷积提取或增加特征的卷积层，都是遵循``Conv2d -> BatchNorm2d -> Activation（SiLU或ReLU）``的组合。
原因如下：
1. 卷积层（Convolution）
	作用：通过局部感受野与权值共享提取空间特征（边缘、纹理、语义模式），可改变通道数与空间分辨率（通过步幅/空洞率）；
2. 归一化层（Normalization）
	作用：规范化激活的统计（均值/方差），缓解内部协变量偏移，稳定训练，加速收敛；附带的可学习仿射参数（γ、β）保证表达能力。
3. 激活函数（Activation）
	作用：引入非线性，提升表达力。
值得强调的是，简单任务时，顺序互相改不会影响太多结果，**但不同顺序会有不同的影响**。
4. Conv → Norm → Act（后归一化，Post-Norm）
	流程：卷积提特征 → 归一化卷积输出 → 激活引入非线性
	优点：
	稳定性高：激活前做归一化，减小分布漂移，支持更高学习率。
	融合友好：多尺度特征在融合（相加/拼接）前后更易保持一致统计。
	与残差分支/上采样路径配合良好，经验广泛验证。
	适用场景：绝大多数情况下的首选，尤其是你刚开始搭建模型或不确定如何选择时。
5. Norm → Act → Conv（预归一化，Pre-Norm，ResNet-v2/Transformer风格）
	流程：先归一化与激活输入 → 再进入带参数的卷积
	优点：
	卷积层看到更稳定的输入分布；深层网络中梯度更顺畅（配合残差时尤为明显）。
	对 batch 尺度不敏感。
	适用场景：研究或复现特定的先进架构时可以参考，但对于YOLO这类网络，并不是主流选择。仅限极深残差网络的残差分支入口，即跳跃链接的位置距离输入非常远，用于优化梯度回传
6. Norm → Conv → Act（不推荐）
	流程：先归一化，再卷积，最后激活
7. Conv → Act → Norm（不推荐）
### 总结
- 它接收什么：
- 它怎样逐步提取特征：
- 哪些尺度或stride输出给后续：
- 分辨率、通道和语义大致怎样变化：
- 小目标可能在哪一步丢失，为什么：
- 只加深或加宽Backbone为什么未必解决该问题：
- 我的证据、例子或疑问：

### 3. 多尺度特征与Neck

- Backbone给Neck哪些特征：
- Neck怎样连接不同尺度：
- 上采样、下采样、拼接或相加各自改变什么：
- 高分辨率位置细节与低分辨率语义怎样互补：
- P2、P3、P4、P5分别可能服务哪些目标尺度：
- 增加P2或移除P5可能带来什么收益和代价：
- 我的证据、例子或疑问：

### 4. Detection Head

- Head接收什么：
- 分类与框回归分别输出什么：
- 多尺度Head之间是独立还是共享；当前版本的证据是什么：
- 原始输出在训练和推理中分别去向哪里：
- Head错误可能表现为分类错、定位差、重复框还是漏检：
- 这与练手论文B0/B1/B2/M的关系：
- 我的证据、例子或疑问：

### 5. 训练分支

- 真值标注怎样进入训练链：
- 标签分配决定了什么：
- 分类、定位和其他损失各自惩罚什么：
- Loss怎样通过反向传播影响Backbone、Neck和Head：
- 类别不平衡或尺度不平衡可能造成什么现象：
- 哪些内容是通用YOLO概念，哪些必须核对具体版本源码：
- 我的证据、例子或疑问：

### 6. 推理分支

- Head原始输出怎样解码成候选框：
- 置信度筛选改变什么：
- NMS接收什么、比较什么、删除什么：
- 置信度阈值和NMS IoU阈值怎样影响Precision、Recall和候选数：
- 最终输出包含哪些字段：
- 哪些后处理设置必须在公平实验中固定：
- 我的NMS手算或疑问：

### 7. 评价指标

不必重新抄写所有公式。用自己的话解释含义，并至少完成一个小算例。

- IoU回答什么问题：
- TP、FP、FN怎样由预测和真值匹配得到：
- Precision高或低分别意味着什么：
- Recall高或低分别意味着什么：
- PR曲线为什么比单一阈值更完整：
- AP、mAP50和mAP50–95的关系：
- AP_small与Recall_small各自能发现什么：
- 为什么参数量、GFLOPs和真实延迟不能互相替代：

我的手算：

- 已知条件：
- 计算过程：
- 结果：
- 我从结果中学到什么：

### 8. 从错误回到结构

每次只记录一个真实或假设现象，用因果链写，不必先做大表。

> 观察到的现象 → 可能发生问题的环节 → 还缺什么证据 → 可否通过最小对照验证

错误记录模板：

- 观察到的现象：
- 错误类型：漏检 / 误检 / 分类错 / 定位差 / 重复框
- 我怀疑的环节及理由：
- 其他可能解释：
- 判断所需证据：
- 当前是否足以触发结构改动：否

## ✅ 学完知识链后回答的必学问题

这些问题用于验收，不用于替代前面的学习过程。没有证据时可以写“暂不能回答”。

1. 请从输入开始，连续解释YOLO训练阶段的完整数据流。

   我的回答：

2. 请从输入开始，连续解释YOLO推理阶段的完整数据流。

   我的回答：

3. Backbone、Neck和Head各自解决什么问题？它们怎样相互连接？

   我的回答：

4. 小目标可能在哪些下采样或融合环节丢失？需要什么证据才能定位？

   我的回答：

5. 提高输入分辨率、加入P2和加深Backbone的作用与代价有什么不同？

   我的回答：

6. 标签分配、分类损失和定位损失分别可能造成哪些错误？

   我的回答：

7. 置信度阈值和NMS IoU阈值怎样共同改变Precision、Recall和候选数量？

   我的回答：

8. 为什么mAP50不能替代mAP50–95？为什么本论文还需要AP_small与Recall_small？

   我的回答：

9. 怎样公平比较B0、B1、B2和M？哪些条件必须固定？

   我的回答：

10. 哪类错误证据才足以触发一个检测头改动？

    我的回答：

## 💡 填写示例：Backbone一小段

<details>
<summary><strong>📋 只看记录格式，不把示例当成标准答案</strong></summary>

- 它接收什么：经过预处理的图像张量。
- 它做什么：逐层形成不同分辨率的特征；具体模块名称需要核对所学YOLO版本。
- 它输出什么：若干尺度特征，交给Neck继续融合。
- 与小目标的关系：我目前推测多次下采样可能削弱小目标信息，但还需要结合特征尺寸或可视化验证。
- 我的证据：课程结构图第X页；对应源码位置待补。
- 我的疑问：具体哪些层输出给Neck？不同YOLO版本是否一致？

</details>

---

这个格式允许先写“我现在理解到哪里”，而不是一开始就填写完整结论。后续查到源码或实验结果时，只修正相关环节。

## 🎯 本笔记的最小完成证据

- [ ] 能沿结构连续讲清训练链和推理链
- [ ] 结构图已完成或已在附件源码上标注
- [ ] 至少记录一次特征尺度变化
- [ ] 完成一组IoU、Precision、Recall手算
- [ ] 完成一组NMS手算
- [ ] 用自己的话解释mAP50与mAP50–95的区别
- [ ] 至少写出两个“错误现象→结构环节→所需证据”链条
- [ ] 十个必学问题均已回答或明确标记为待查证

## 🚫 当前边界

- 不背诵YOLO全部版本历史
- 不预先填写未经核验的具体模块名、通道数、默认阈值或损失配置
- 不下载正式数据
- 不运行正式训练
- 不修改网络
- 不把注意力、损失、增强或其他模块加入练手论文

## 📋 完成评审

- 当前状态：未评审
- 我最有把握的环节：
- 我仍然无法连接的环节：
- 本轮形成的文件或证据：
- 是否通过S0-01：否
- 通过后下一任务：S0-02 PyTorch与可复现性
