2026.06.12 - 2026.06.21学习记录
1. 尝试在google colab平台进行python的目标检测调试。该平台主要用于显卡（T4）免费使用，一周30个小时左右。主要工作如下。
- 注册并登录 Colab，创建 Python 笔记本，通过 `!nvidia-smi` 确认分配了 T4 显卡。  
- 挂载 Google Drive 保存数据集和权重，编写脚本安装 PyTorch 和 torchvision。  
- 使用预训练的 Faster R‑CNN 对单张图片进行推理测试，验证 GPU 加速效果。  
- 记录会话闲置超时和断开连接会终止任务的规律。
2. 尝试使用kaggle平台的GPU进行代码仿真。和google colab区别在于，kaggle退出会话可继续执行模型任务，但google colab断开网页连接则直接停止任务。主要工作如下。
- 在 Kaggle 创建 Notebook，启用 GPU 加速器，安装所需依赖库。  
- 编写训练循环，测试关闭浏览器后任务仍在后台继续执行的功能。  
- 将数据集通过 Kaggle Dataset 添加，训练完成后将权重保存到外部存储或重新上传。  
- 对比两平台运行方式，确定调试用 Colab、正式训练用 Kaggle 的分工。
3. 完成深度学习环境的 YOLO「训练 - 验证 - 推理」搭建。
- 准备 COCO 子集数据，转换为 YOLO 格式的标签和 `data.yaml` 配置文件，按比例划分训练/验证/测试集。  
- 基于 YOLOv8n 在 Kaggle 上启动训练（batch=16, epochs=50，启用早停），保存最佳权重 `best.pt`。  	
- 在 Colab 上加载权重，执行 `model.val()` 计算验证集 mAP 指标。  
- 编写推理脚本，对单张图片和视频进行目标检测，输出带框的结果图，并尝试导出 ONNX 格式。

