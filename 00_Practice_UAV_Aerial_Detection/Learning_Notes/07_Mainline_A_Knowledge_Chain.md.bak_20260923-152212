# 07 主线 A 知识链：预算–切片–分辨率（P0 核心）

> 关卡：L3｜过关只查**第二部分**｜[`../Learning_Check_Baseline.md`](../Learning_Check_Baseline.md)  
> 本块是练手 EI 稿的**主知识链**；对应五方法与 Stage A–F 纪律。

---

## 第一部分：必须掌握

### 学习目标

1. 闭卷画出五方法数据流，并标出「唯一改动的环节」。
2. 能解释选区 vs oracle、坐标还原、融合 NMS、整帧预算之间的依赖。
3. 能把已完成的 Stage A–D/F 证据接到 C1/C2，并说出 Stage E 缺什么。

### 知识链

```text
冻结权重 last.pt
→ 方法协议（F640 / F1280 / DensK1 / UnifAll / SAHI640）
→ （可选）切片与坐标映射
→ 推理与 NMS / 融合
→ 原图空间评价（小目标）
→ 整帧耗时与超时
→ 图级配对统计（Stage F）
→ 主张措辞（C1/C2）
```

### 五方法（只改协议，不改网络）

| 方法 | 核心操作 | 主要换取 | 主要代价 |
|---|---|---|---|
| F640 | 整图 imgsz=640 | 快 | 小目标易丢 |
| F1280 | 整图 imgsz=1280 | 更高有效分辨率 | 算力升 |
| DensK1 | 密度引导单片+底图 | 局部加精 | 选区失误→漏检 |
| UnifAll | 均匀全覆盖切片 | 覆盖全 | 延迟高 |
| SAHI640 | SAHI 切片协议 | 工程化切片 | 延迟很高 |

DensK1 定义以 BTD8 冻结为准；未经授权不得改网络或重训。

### 链上要点

1. **Oracle 禁令：** 用 GT 引导选区得到的召回，不得写成可部署方法精度（C2）。
2. **Stage 门：** A 环境冻结 → B 计时 → C cal48 指标 → D test-dev 一次性 → F 图级统计；E 外数据集可选且现阻塞于 UAVDT 落盘。
3. **主比较：** 正文主叙事优先 F1280 vs DensK1；UnifAll/SAHI 作预算边界对照。
4. **披露：** 忽略区、Unknown 源、开发集 cal48、硬件型号。

### 练习

1. 自绘五方法流程图（可 Mermaid 或手绘拍照链到本笔记）。
2. 说明：为何 Stage D 之后才能跑 Stage F，且 F 不得回头改 D 的 conf。
3. 对照 Stage F：F1280 vs DensK1 的 Δrecall_small CI 不过 0，对 C1 意味着什么？对「DensK1 可部署替代 F1280」意味着什么？

### 必答题

1. DensK1 相对 UnifAll，在「可部署」叙事上多了什么风险？
2. 为什么说 P0 是协议论文而不是新检测器论文？
3. Stage E 未完成时，外推到「任意无人机数据集」的句子哪些必须删除？

---

## 第二部分：我的记录

### 元信息

| 字段 | 填写 |
|---|---|
| 开始日期 | |
| 状态 | `TODO` / `LEARNING` / `REVIEWING` / `PASSED` / `REPEAT` |

### 闭卷串讲（必须覆盖五方法）

- 我的复述：

### 练习证据

| 练习 | 证据 | 摘要 |
|---|---|---|
| 五方法图 | | |
| Stage 纪律 | | |
| C1 解读 | | |

### 必答初稿

1.
2.
3.

### 订正 / 不确定 / 助手检查区

-
