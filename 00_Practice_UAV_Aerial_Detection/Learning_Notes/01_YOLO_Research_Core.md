# 学习笔记1：YOLO研究核心

_沿YOLO结构建立知识链；先记录理解，再集中回答必学问题。_

---

统一原则见[项目学习笔记统一记录方法](../../00_Overview/Learning_Note_Method.md)。
## 🎯 必须掌握

1. **完整检测链及训练/推理分支**
   - 复述要求：闭卷讲清输入如何经过Backbone、Neck和Head，并分别进入训练与推理分支。
   - 验证方式：独立画出结构图，为每个节点标出输入、处理和输出。
2. **预测成为损失或最终检测结果的机制**
   - 复述要求：讲清标签分配、损失计算、框解码和NMS分别解决什么问题及其先后关系。
   - 验证方式：完成一组IoU、Precision、Recall和NMS手算，并解释AP与mAP的评价范围。
3. **从错误证据回到结构环节**
   - 复述要求：区分漏检、误检、定位偏差和重复框，并说明可能关联的环节。
   - 验证方式：针对至少两个错误案例写出“现象→证据→候选原因→是否改结构”的判断链。

## 🔗 主题知识链

按下面的连接关系组织学习：

> 输入与预处理 → Backbone → 多尺度特征 → Neck → Detection Head → 训练分支 / 推理分支 → 评价指标 → 错误证据 → 是否需要结构改动

学习时始终追问四件事：

1. 这一环节接收什么？
2. 它做了什么？
3. 它输出什么给下一环节？
4. 如果这里出错，最终会出现什么可观察现象？

## ✍️ 灵活学习记录

不要维护复杂表格。每次学习复制下面四行即可：

### YOLO8

- 本次只学：YOLO8
- 使用资料及版本：[[万字详解YOLOv8网络结构]]
- 新增或修正的理解：
- 留下的证据或问题：

## 📚 按结构填写知识链

**Conv：** 提取特征+下采样提取特征+下采样

**C2f：** 多路径特征复用+改善梯度传播多路径特征复用+改善梯度传播

**SPPF：** 扩大感受野扩大感受野

**FPN：** 深层语义→浅层深层语义\rightarrow浅层

**PAN：** 浅层定位→深层浅层定位\rightarrow深层

**P3/P4/P5：** 小/中/大目标小/中/大目标

**Detect Head：** 分类+定位

### 1. 输入与预处理

- 我看到的原始输入是什么：图形尺寸为`[640, 640, 3]`
- 进入模型前发生了什么：会进行反转、对称、模糊等等的预处理，可以增大数据量和防止过拟合
- 输入张量或尺寸怎样表示：`[64, 640, 640, 3]`，64是批次
- 哪些处理只在训练出现，哪些也在推理出现：BackBone、Neck、Head都会出现，区别在于训练会在分类和瞄框时做损失函数
- 这一环节可能制造的数据偏差或泄漏：不清楚

### 2. Backbone 特征提取
基本组成是使用一系列卷积和反卷积层。基本使用如下。
1. Cond2d卷积[[3 模型构建]]
2. BatchNorm2d层：归一化[[6 正则化Regularization]]
3. SiLU激活函数:关键特性是它允许平滑的梯度
4. **瓶颈块Bottleneck Block**
	1. 基本思想：残差块[[99_Attachments/Archive_2026-09-16_PracticePaper/00_PrePaper_Lightweight_Detection/Learning_Notes/00_ResNet_Core]]，每个Bottleneck Block也是残差卷积，输出一个映射
	2. 本质：每个Bottleneck Block是**3×3 → 3×3** 卷积，通过两个卷积把维度配置到想要的某个值，在配置项里面可以应用经典的**分组卷积、深度可分离卷积**等等，也可以配置最后的输出是否进行跳跃链接（残差配置）
#### 2.1 卷积块（CBS）

整个YOLO网络里面所涉及的卷积提取或增加特征的卷积层，都是遵循``Conv2d -> BatchNorm2d -> Activation（SiLU或ReLU）``的组合。
> **空间信息减少，语义信息增加。**

这里：``kernel = 3, stride = 2``, stride = 2会让尺寸减半

原因如下：
1. 卷积层（Convolution）
	作用：通过局部感受野与权值共享提取空间特征（边缘、纹理、语义模式），可改变通道数与空间分辨率（通过步幅/空洞率）；
2. 归一化层（Normalization）
	作用：规范化激活的统计（均值/方差），缓解内部协变量偏移，稳定训练，加速收敛；附带的可学习仿射参数（γ、β）保证表达能力。
3. 激活函数（Activation）
	作用：引入非线性，提升表达力。
值得强调的是，简单任务时，顺序互相改不会影响太多结果，**但不同顺序会有不同的影响**。
4. Conv → Norm → Act（后归一化，Post-Norm）
	流程：卷积提特征 → 归一化卷积输出 → 激活引入非线性
	优点：
	稳定性高：激活前做归一化，减小分布漂移，支持更高学习率。
	融合友好：多尺度特征在融合（相加/拼接）前后更易保持一致统计。
	与残差分支/上采样路径配合良好，经验广泛验证。
	适用场景：绝大多数情况下的首选，尤其是你刚开始搭建模型或不确定如何选择时。
5. Norm → Act → Conv（预归一化，Pre-Norm，ResNet-v2/Transformer风格）
	流程：先归一化与激活输入 → 再进入带参数的卷积
	优点：
	卷积层看到更稳定的输入分布；深层网络中梯度更顺畅（配合残差时尤为明显）。
	对 batch 尺度不敏感。
	适用场景：研究或复现特定的先进架构时可以参考，但对于YOLO这类网络，并不是主流选择。仅限极深残差网络的残差分支入口，即跳跃链接的位置距离输入非常远，用于优化梯度回传
6. Norm → Conv → Act（不推荐）
	流程：先归一化，再卷积，最后激活
7. Conv → Act → Norm（不推荐）
#### 2.2 C2f模块 基本单元

 **结构如下**。
1. Conv2d，1×1卷积：接收输入特征图，生成中间特征图
2. 中间图拆分（一般是对半），一部分直接送到最后的concat模块，另一部分经过多个瓶颈块在送到concat模块。瓶颈块根据实际情况可调整复杂度
3. concat将中间图融合，融合的是通道，即通道扩容
	这样做的原因是。**降低模块计算量和参数量**（CSPNet（Cross Stage Partial Network）思想）；**短路径**保留了**低级别细节特征**，**长路径**则提取了**高级别语义特征**（ELAN（Efficient Layer Aggregation Network）的思想），同时利用低级别细节和高级别语义特征。
4. 核心过程大概如下。输入 $X$ 进来，经过一个$1×1$ 卷积，拆成$X_1$和$X_2$，  $X_2$在经过3层Bottleneck，每层张量大小不变，得到 $B_1/B_2/B_3$，B1 = 浅层纹理，B2 = 局部结构，B3 = 更复杂语义，**让不同深度的特征都能够直接进入最终融合**，即$Concat(X1​,X2​,B1​,B2​,B3​)$ 在用一个$1×1$ 卷积和$X_1$融合。
5. 启发思想。
	1. 不同深度的特征都能够直接进入最终融合。可以**特征复用**、**改善梯度传播**
	2. 使用1×1 卷积对同一个空间位置上的所有特征重新组合。可以调整通道、压缩通道、融合通道、降低计算量。
#### 2.3 模块组成
backbone对应到yolo的yaml文件中的：
```yaml
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]  # 0-P1/2 第0层，-1代表将上层的输入作为本层的输入。第0层的输入是640*640*3的图像。Conv代表卷积层，相应的参数：64代表输出通道数，3代表卷积核大小k，2代表stride步长。
  - [-1, 1, Conv, [128, 3, 2]]  # 1-P2/4 第1层，本层和上一层是一样的操作（128代表输出通道数，3代表卷积核大小k，2代表stride步长）
  - [-1, 3, C2f, [128, True]] # 第2层，本层是C2f模块，3代表本层重复3次。128代表输出通道数，True表示Bottleneck有shortcut。
  - [-1, 1, Conv, [256, 3, 2]]  # P3 3-P3/8 第3层，进行卷积操作（256代表输出通道数，3代表卷积核大小k，2代表stride步长），输出特征图尺寸为80*80*256（卷积的参数都没变，所以都是长宽变成原来的1/2，和之前一样），特征图的长宽已经变成输入图像的1/8。
  - [-1, 6, C2f, [256, True]] # 第4层，本层是C2f模块，可以参考第2层的讲解。6代表本层重复6次。256代表输出通道数，True表示Bottleneck有shortcut。经过这层之后，特征图尺寸依旧是80*80*256。
  - [-1, 1, Conv, [512, 3, 2]]  # P5 5-P4/16 第5层，进行卷积操作（512代表输出通道数，3代表卷积核大小k，2代表stride步长），输出特征图尺寸为40*40*512（卷积的参数都没变，所以都是长宽变成原来的1/2，和之前一样），特征图的长宽已经变成输入图像的1/16。
  - [-1, 6, C2f, [512, True]] # 第6层，本层是C2f模块，可以参考第2层的讲解。6代表本层重复6次。512代表输出通道数，True表示Bottleneck有shortcut。经过这层之后，特征图尺寸依旧是40*40*512。
  - [-1, 1, Conv, [1024, 3, 2]]  # P5 7-P5/32 第7层，进行卷积操作（1024代表输出通道数，3代表卷积核大小k，2代表stride步长），输出特征图尺寸为20*20*1024（卷积的参数都没变，所以都是长宽变成原来的1/2，和之前一样），特征图的长宽已经变成输入图像的1/32。
  - [-1, 3, C2f, [1024, True]] #第8层，本层是C2f模块，可以参考第2层的讲解。3代表本层重复3次。1024代表输出通道数，True表示Bottleneck有shortcut。经过这层之后，特征图尺寸依旧是20*20*1024。
  - [-1, 1, SPPF, [1024, 5]]  # 9 第9层，本层是快速空间金字塔池化层（SPPF）。1024代表输出通道数，5代表池化核大小k。结合模块结构图和代码可以看出，最后concat得到的特征图尺寸是20*20*（512*4），经过一次Conv得到20*20*1024。
```

```
P3 = 80×80  → 细节多
P4 = 40×40  → 中间
P5 = 20×20  → 语义强

P3：目标位置
“这里好像有一条细线。”
位置准
语义弱

P4：
“这条线可能属于一个裂纹结构。”

P5：目标识别
“这是一个完整的缺陷区域。”
语义强
位置粗
```

#### 2.4 SPPF 快速空间金字塔池化层

用于不同尺度的池化操作，将不同尺度的特征图拼接在一起，提高对不同尺寸目标的检测能力。操作步骤如下。
	1. 初始化模块：输入通道和输出通道相同，用个卷积$1×1$+批归一化+激活函数初步提取特征
	2. 3个$5×5$最大池化层，输出后在通道拼接，特征图之所以没有任何变化因为池化层做了padding=2，输入和输出就一样
	3. 在用一个卷积$1×1$+批归一化+激活函数输出一个和输入通道相同的特征图
	4. **设计原因**：
		1. 第一次卷积，压缩通道，节省算力。
		2. 3个最大池化层，可以极大的扩大感受野，计算成本非常低。
		3. 结尾卷积，衔接下一层，并进行通道混合
	5. 放在最后有助于网络理解: 这个黑线只是纹理？还是一条连续裂纹？还是轨道结构？
	6. 也就是说，这里底层是C大，HW小，即**深层网络，空间感较弱，但语义较强的特征**，高层是HW大，C小，即**空间感较强，但语义较少的特征**

#### 2.5 总结

- 它接收什么：3维图像
- 它怎样逐步提取特征：先Conv2d两层压缩特征，用C2f提取纹理和语义并融合，在压缩在融合重复这个过程
- 哪些尺度或stride输出给后续：P3，5，7后续进入Neck，注意：除了正常Conv，其他的结构（C2f，SPPF）都需要保证输入和输出通道一致，即padding≠0
- 分辨率、通道和语义大致怎样变化：分辨率降低，通道增大，语义变强，但位置变弱，所以需要融合高分辨率
- 小目标可能在哪一步丢失，为什么：最后几层Conv，因为感受野越来越大，小目标特征也就越来越模糊
- 只加深或加宽Backbone为什么未必解决该问题：加深只能获取语义，但细节丢失的就更多了
- 我的证据、例子或疑问：/

### 3. 多尺度特征与Neck

采用的结构是：**$FPN + PAN$**

#### 3.1 $FPN$ 特征金字塔网络 

$FPN$底层上采样然后和对应的低层特征进行融合，简单来讲：
1. 一个特征图是H、W逐渐变小，C逐渐变大，变成底层
2. 在C逐渐变大过程中，有C1、C2、C3、C4
3. C4逐渐上采样变小，又变成C41、C42、C43
4. C1、C2、C3和C41、C42、C43拼接
5. 拼接过程中还要来C2f模块，把拼接$[C1，C4_1]$的语义特征和细节特征融合在一起向上传，即学习“深层语义”和“浅层空间细节”之间应该怎么组合，**==把高级语义向高分辨率层传播==**
6. 这里主要是从深层往浅层传

#### 3.2  $PAN$  路径聚合网络
$PAN$从底层向上和高层特征进行融合，即：
1. 接上面，继续卷积，C逐渐变大，变大过程中，逐渐与上一步逐渐变小的C41、C42、C43逐渐拼接
2. 这一层是浅层往深层传
3. 之所以是拿上一步的C41、C42、C43，是因为 PAN 需要的是“已经融合过语义信息的特征”，而不是 Backbone 中单纯的原始特征。

#### 3.3 总结

- Backbone给Neck哪些特征：主要是底层的语义特征，上层的位置和细节特征是通过上采样拼接的
- Neck怎样连接不同尺度：上下采样（最近邻）
- 上采样、下采样、拼接或相加各自改变什么：补充互相没有的语义或者细节特征
- 高分辨率位置细节与低分辨率语义怎样互补：用C2f将对方没有的特征加在一起
- P2、P3、P4、P5分别可能服务哪些目标尺度：小、中、大
- 增加P2或移除P5可能带来什么收益和代价：很明显增加小目标识别指标，降低大目标指标
- 我的证据、例子或疑问：

### 4. Detection Head

Head部分负责将Neck部分输出的特征进行进一步处理，以生成最终的检测结果。主要功能是将特征图转换为目标检测所需的具体信息，包括类别、位置和置信度。

#### 4.1 结构

这部分共有三个检测块组成，即检测大中小三个目标。每个检测块都包含下面两个网络部分。分别预测框和分类。
- **检测头** ：负责**确定物体在哪里**。它预测的是边界框的坐标信息。这些层负责预测每个锚框的边界框回归值和目标存在的置信度。
- **分类头** ：负责**识别物体是什么**（如“裂纹”、“螺栓”）。它输出的是目标属于每个预设类别的概率。
- **两个头都具备下面网络模型结构**： Conv + Conv + nn.Conv2d
	1. **两个卷积块** （每个轨道）:
		- 每个卷积块包含以下组件：
			- 卷积层（Conv2d）
			- 批归一化（BatchNorm2d）
			- 激活函数（通常是SiLU或ReLU）
	2. **单独的Conv2d层** （每个轨道）:
		- 用于边界框预测的轨道输出一个Conv2d层，得到边界框损失。
		- 用于类别预测的轨道输出一个Conv2d层，得到类别损失。
	3. 三个检测块**输出结果**：
		- 输出是的特征尺寸分别为80x80，40x40，20x20
		- 分别对应小、中、大目标检测
		- 网格数分别为6400，1600，400，因为**尺寸越小，感受野越大**
		- 因为预测方式是无框预测，所以将每个输入的特征图中的**像素点都看作为一个先验点**，对其直接预测，包括类别概率和边框坐标
	4. 为什么分开（yolo5是合起来）：两个功能是独立的，定位检测是关注空间内容，分类关注语义内容

#### 4.2 工作步骤

1. 解耦头的设计。见上述网络结构。
2. 无锚框 (Anchor-Free) 的预测方式
	将输入特征图的**每个像素点**都视为一个**先验点**。对于每个点，Head会直接预测：
	- 如果该点是目标的**中心**，则负责预测这个目标。
	- 预测内容包括：**边界框的中心点坐标 (x, y)** 和**边界框的宽高 (w, h)**
3.  原始输出与“解码”过程
	- **训练时**：Head输出的是用于计算损失的**原始数据**，并非最终坐标。
	- **推理/部署时**：这些原始数据需要经过一个“**解码**”过程才能变成最终的检测框
	- 解码过程：以YOLOv8导出ONNX模型为例，其输出通常包含**9个张量**，对应**3个检测头**（负责不同尺度目标）各输出的**3个分支**，也是三个损失函数的计算方式
4. **边界框分支 (Box)**：输出形状为 `[1, 64, H, W]`。这里的 **64** 是 **4个边界值**（左、上、右、下）各用**16个概率**来表示。解码时，会对这16个数做Softmax，再加权求和得到一个精确的偏移量。这种精细的**分布表示**，是其精度提升的关键之一。
5. **分类分支 (Cls)**：输出形状为 `[1, NC, H, W]`（NC是类别数）。对这些值应用 **Sigmoid** 函数，即可得到0到1之间的**类别概率**。这里的概率已隐含了“是否有物体”的信息，所以无需额外的置信度。
6. **DFL（Distribution Focal Loss）分支**：用于边界框预测。
	1. 网络很难精确学习连续数值，需要把距离变成概率分布。
	2. 每个边界值都有对应的一个概率值，通过算数学期望得到最终的数值
7. 后处理之非极大值抑制 (NMS)
	1. 得到**8400个**预测框，其中很多是高度重叠或错误的。因此，最后一步是进行**非极大值抑制（NMS）** 后处理。
	2. NMS的核心逻辑是“抑制”掉那些**重叠度（IoU）高但置信度低**的冗余框，最终为每个目标只保留一个最精确的检测框

- Head接收什么：三个大中小的特征图
- 分类与框回归分别输出什么：目标概率以及框的边界
- 多尺度Head之间是独立还是共享；当前版本的证据是什么：不知道什么意思？？
- 原始输出在训练和推理中分别去向哪里：不清楚
- Head错误可能表现为分类错、定位差、重复框还是漏检：分类错、定位差、漏检，重复框会通过NMS筛除掉
- 我的证据、例子或疑问：/

### 5. 训练分支

- 真值标注怎样进入训练链：
- 标签分配决定了什么：
- 分类、定位和其他损失各自惩罚什么：
- Loss怎样通过反向传播影响Backbone、Neck和Head：
- 类别不平衡或尺度不平衡可能造成什么现象：
- 哪些内容是通用YOLO概念，哪些必须核对具体版本源码：
- 我的证据、例子或疑问：

### 6. 推理分支

- Head原始输出怎样解码成候选框：
- 置信度筛选改变什么：
- NMS接收什么、比较什么、删除什么：
- 置信度阈值和NMS IoU阈值怎样影响Precision、Recall和候选数：
- 最终输出包含哪些字段：
- 哪些后处理设置必须在公平实验中固定：
- 我的NMS手算或疑问：

### 7. 评价指标

不必重新抄写所有公式。用自己的话解释含义，并至少完成一个小算例。

- IoU回答什么问题：
- TP、FP、FN怎样由预测和真值匹配得到：
- Precision高或低分别意味着什么：
- Recall高或低分别意味着什么：
- PR曲线为什么比单一阈值更完整：
- AP、mAP50和mAP50–95的关系：
- AP_small与Recall_small各自能发现什么：
- 为什么参数量、GFLOPs和真实延迟不能互相替代：

我的手算：

- 已知条件：
- 计算过程：
- 结果：
- 我从结果中学到什么：

### 8. 从错误回到结构

每次只记录一个真实或假设现象，用因果链写，不必先做大表。

> 观察到的现象 → 可能发生问题的环节 → 还缺什么证据 → 可否通过最小对照验证

错误记录模板：

- 观察到的现象：
- 错误类型：漏检 / 误检 / 分类错 / 定位差 / 重复框
- 我怀疑的环节及理由：
- 其他可能解释：
- 判断所需证据：
- 当前是否足以触发结构改动：否

## ✅ 学完知识链后回答的必学问题

这些问题用于验收，不用于替代前面的学习过程。没有证据时可以写“暂不能回答”。

1. 请从输入开始，连续解释YOLO训练阶段的完整数据流。

   我的回答：

2. 请从输入开始，连续解释YOLO推理阶段的完整数据流。

   我的回答：

3. Backbone、Neck和Head各自解决什么问题？它们怎样相互连接？

   我的回答：

4. 小目标可能在哪些下采样或融合环节丢失？需要什么证据才能定位？

   我的回答：

5. 提高输入分辨率、加入P2和加深Backbone的作用与代价有什么不同？

   我的回答：

6. 标签分配、分类损失和定位损失分别可能造成哪些错误？

   我的回答：

7. 置信度阈值和NMS IoU阈值怎样共同改变Precision、Recall和候选数量？

   我的回答：

8. 为什么mAP50不能替代mAP50–95？为什么本论文还需要AP_small与Recall_small？

   我的回答：

9. 怎样公平比较B0、B1、B2和M？哪些条件必须固定？

   我的回答：

10. 哪类错误证据才足以触发一个检测头改动？

    我的回答：

## 💡 填写示例：Backbone一小段

<details>
<summary><strong>📋 只看记录格式，不把示例当成标准答案</strong></summary>

- 它接收什么：经过预处理的图像张量。
- 它做什么：逐层形成不同分辨率的特征；具体模块名称需要核对所学YOLO版本。
- 它输出什么：若干尺度特征，交给Neck继续融合。
- 与小目标的关系：我目前推测多次下采样可能削弱小目标信息，但还需要结合特征尺寸或可视化验证。
- 我的证据：课程结构图第X页；对应源码位置待补。
- 我的疑问：具体哪些层输出给Neck？不同YOLO版本是否一致？

</details>

---

这个格式允许先写“我现在理解到哪里”，而不是一开始就填写完整结论。后续查到源码或实验结果时，只修正相关环节。

## 🎯 本笔记的最小完成证据

- [ ] 能沿结构连续讲清训练链和推理链
- [ ] 结构图已完成或已在附件源码上标注
- [ ] 至少记录一次特征尺度变化
- [ ] 完成一组IoU、Precision、Recall手算
- [ ] 完成一组NMS手算
- [ ] 用自己的话解释mAP50与mAP50–95的区别
- [ ] 至少写出两个“错误现象→结构环节→所需证据”链条
- [ ] 十个必学问题均已回答或明确标记为待查证

## 🚫 当前边界

- 不背诵YOLO全部版本历史
- 不预先填写未经核验的具体模块名、通道数、默认阈值或损失配置
- 不下载正式数据
- 不运行正式训练
- 不修改网络
- 不把注意力、损失、增强或其他模块加入练手论文

## 📋 完成评审

- 当前状态：未评审
- 我最有把握的环节：
- 我仍然无法连接的环节：
- 本轮形成的文件或证据：
- 是否通过S0-01：否
- 通过后下一任务：S0-02 PyTorch与可复现性
