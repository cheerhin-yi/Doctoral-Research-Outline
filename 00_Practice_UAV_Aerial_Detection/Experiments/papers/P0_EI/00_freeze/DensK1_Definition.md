# DensK1 definition extract (from BTD8; not redefined)

## Source: 00_Practice_UAV_Aerial_Detection/Experiments/BTD8_Single_Crop_Protocol.md

- L5: 缓存阶段：核验BTD1预测/记录、cal48图像与标注SHA及BTD7第一片结果。枚举240个既有局部窗口的NMS(全图+该片)，沿用原评价规则、conf=.25/IoU=.5、原图面积<1024和ignore语义。密度第一片固定为粗预测conf≥.25中心最多的窗口，同分索引；GT最佳单片按最终小TP最大，同分索引，不优化FP。另列允许不切片的GT诊断上界。保存每窗口匹配原标注行号、相对密度第一片补回/丢失、FP、最佳窗口与类别统计。GT选择不是方法，不具有可部署性，也不能直接套用密度单片的计时。
- L7: 计时阶段：固定100轮末EMA、GTX1660SUPER/FP32/batch1，640原像素窗口、原步长512及贴边/短边填充。全图输入640，局部640；每视图conf=.001/max1000，公共CPU NMS沿用BTD5实现，类间独立/IoU>.5/max500。仅将BTD6两片改为密度Top1；F1280只作时间参考，不新增其准确率分析。
- L9: 先前三张各方法检查通路，再各预热20次。cal48两方法各3次真实前向，共288次计时；图像/重复序号决定K1与F1280交替先后，避免原先参考恒在最后的顺序偏差。每次K1选区和最终输出对照缓存，形状/行序一致、atol=1e-5/rtol=0；记录精确相等数。F1280重复输出按同容差校验。不匹配立即停止，保存失败，不改阈值。
- L11: 边界为已解码CPU RGB到最终CPU框，包括转换、窗口、全图检测/公共NMS、密度选择、裁片/检测/回映/合并；前后GPU同步。磁盘读取/解码、匹配、缓存比对、资源采样和写盘均在计时外。每次真实计算，禁止缓存充当测速。T仍按cal48 F1280每图3次中位数p95向上取整，不沿用40ms；报告全部帧次与逐图中位数的超参考率、完整时间分布和分项，不声称硬实时或业务截止时间。
- L15: 验收：48图/240单片组合、逐图/逐类小TP和FP、固定简单规则与GT上界差及损失；288条真实计时、输出一致性、重新校准T、资源记录/快照/SHA；结论区分动作预算与选择空间。若单片仍明显超参考或GT上界接近简单选择，暂缓当前配置下区域排序作为唯一创新，先给出研究问题调整依据；不自动换网络/模块/窗口。若存在空间，也只开放针对性差异审查，不等于新信号、独立泛化或新颖性通过。

## Source: 00_Practice_UAV_Aerial_Detection/Experiments/BTD8_Single_Crop_Result.md

- L7: cal48的48图、2720个有效小GT，固定全部240窗口；所有单片均与全图按原NMS和评价规则融合。密度第一片为粗预测中心最多者，同分索引；GT最佳单片仅按最终小TP最大、同分索引，不优化FP。
- L11: | 原640全图 | 840 | 30.88% | 0 | 本表不另统计 |
- L23: GTX1660SUPER、100轮末EMA、FP32/batch1、全图640及局部640。只在cal48运行密度K1和整图F1280，每方法48图×3次，共288条真实计时；前有3图通路检查和每方法20次预热。两方法交替先后，F1280不再恒放最后。
- L25: 边界为已解码CPU RGB到最终CPU原图框，包括转换、窗口、全图检测及公共NMS、密度选择、裁片/检测/回映、合并，前后GPU同步。读取/解码、资源采样、输出校核及写盘在计时外。缓存阶段約6.80秒、计时阶段约143.03秒（包含环境加载/预热等），不把阶段总耗时当作单帧时间。
- L29: | 完整时间指标 | 密度K1 | F1280参考 |
- L42: K1平均分项：转换/窗口0.80ms、全图检测及公共后处理17.81ms、选区0.16ms、局部裁剪/检测/回映13.43ms、合并5.16ms。比均值更需要注意的是分布尾部。事后比对发现，10张中位数超40ms图中9张含有会使现有快速NMS回退原实现的缓存全图几何；这只是重合关系，未经因果干预，不证明回退解释全部超时，也不证明修改后一定达标。不能据此断言“单片结构天然无法实时”。本轮未进一步优化后处理。
- L44: F1280平均比密度K1少约10.03ms，但本次只把它作为计时参考，**尚未分析F1280的准确率，不声称它已支配单片方案**。这一缺口比继续给单片排序加信号更应优先检查。
- L48: 144次K1输出全部与缓存逐值相同，最大差0；96次F1280重复输出比较全部精确一致。240窗口的匹配目标集合、补回/丢失、GT最优及同分规则、逐图计数守恒、时间分项总和、参考T和超限率均通过独立校核。第一片匹配集合和FP与BTD7保持一致，原输入SHA不变。
- L54: 证据：[协议](BTD8_Single_Crop_Protocol.md)、[分阶段执行入口](check_single_crop.py)、[结果校核](verify_single_crop.py)。原始产物在被Git忽略的`11_Datasets/processed/VisDrone/BT1/BTD8-SINGLE-20260914-01/`：`cache_summary.json`、`all_240_single_windows.csv`、`all_48_frames.csv`、逐图匹配集合、`summary.json`、`timings.csv`、`resources.jsonl`、`verification.json`、输入SHA与产物清单。`expected_k1_*.npy`/`actual_k1_*.npy`保存单片预测，`reference_F1280_*.npy`保存参考首次预测。
- L60: 下一项 **BTD9：复用已保存F1280预测，完成cal48强基线效果对照**。按既有小目标/ignore口径匹配标注，与原640全图、密度单片、GT最佳单片对照小TP/FP、补回/丢失及类别；使用本次真实F1280时间，但不给GT最佳单片分配未测耗时。无新推理、训练、评分调参或测试集访问。
