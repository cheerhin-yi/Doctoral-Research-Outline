# BTD6：共用快速NMS的整帧校核与预算复测

2026-09-13，`BTD6-PIPELINE-20260913-01`；用户批准继续。唯一实现变化为BTD4中框架预测后的公共NMS替换成BTD5已核验实现；框架内部NMS、权重、数据、选区、窗口、精度与阈值均不变。沿用[BTD4协议](BTD4_Diag500_Protocol.md)的数据、权重SHA及计时边界。旧产物只读，新产物单独保存。

先对cal48全部48图分别运行密度、去重、F1280，在每次公共NMS输入上同时调用原实现与新实现，要求array_equal；覆盖原始框及最终合并输入，失败立即停止。此校核不计入测速。校核通过后关闭参考调用，每配置预热20次；cal48三配置各3次，重新按F1280每图三次中位数的p95向上取整得到T。保留原顺序设计：密度/去重交替，F1280每轮最后；顺序可能带来系统效应，不解读为严格同时配对硬截止。

diag500两方法各3次真实前向，共3000次计时；每次输出及选区均对照BTD4存档。公共NMS校核要求精确相等；独立GPU运行的最终框按原BTD4容差atol=1e-5、rtol=0，形状、行序、选择必须一致，同时记录精确相等次数及最大差。任一容差外差异立即停止，保存失败证据，不改阈值。重新匹配标签仅验证全部逐图TP/FP/小TP及增减结果未变，不进行参数搜索或新的准确率结论。

计时从已解码CPU RGB到最终CPU框，包括转换、全图检测、选区、裁剪/局部检测/回映和合并，前后GPU同步；磁盘解码、资源采样、参考输出比对、匹配与写盘均在计时外。输出全部帧次与逐图中位数的分布及超T比例。T只是同硬件F1280相对参考，不是业务实时期限；不能沿用旧61ms或从孤立算子耗时推算整帧。跨运行速度比较须披露内存/负载差异。

资源：本机GTX1660SUPER/FP32/batch1，不开云端、不训练。每个pipeline开始前采样进程RSS/私有提交内存及系统可用内存，超过RSS 3GiB、私有内存4GiB，或可用内存低于256MiB立即停止；每次失败保留样本。采样不是连续硬限制，短峰值可能漏掉。每帧前检查30分钟总墙钟上限及磁盘至少5GiB，每25图检查本次目录大小不得超过1GiB。OOM或任何非有限结果立即停止。不关闭用户其他程序。记录torch GPU峰值及进程采样最大值，不将其称为整机显存或进程连续峰值。

验收：cal48实际NMS输入精确等价；500图/3000次计时齐全；与旧预测/选择及逐图统计一致；新T、完整耗时与超预算率；输入SHA、脚本/协议快照、资源采样和产物清单。无新训练、网络改动、test-dev或新候选。完成后返回条件新增检出价值的信息缺口审查，不把公共实现加速或简单去重命名为创新。
